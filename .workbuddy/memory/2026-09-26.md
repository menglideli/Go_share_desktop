# 2026-09-26

## 阶段 6 打包分发（全部完成并实测收口）

四项内容全部落地，产物 `dist/goshare-v0.6.0-windows-amd64.exe`（17.7 MB 单文件）：

1. **internal/tray 系统托盘**（纯 syscall）：message-only 窗口 + LockOSThread 消息循环
   goroutine；菜单「显示主窗口/停止分享/退出」每次右键现建现销（状态用原子量）；
   图标 `CreateDIBSection`+`CreateIconIndirect` 程序化绘制（`tray.PaintIcon` 与 exe
   图标同一份代码）。托盘仅交互模式启用（-auto 不启用，避免污染 e2e）。
   像素证据：cmd/traytest + ShowMenuForTest 弹菜单截屏，三项渲染正确、
   「停止分享」SetSharing(true) 后可用。
2. **单 exe 构建**（scripts/build.ps1）：cmd/icon 生成多尺寸 PNG+ICO → go-winres
   嵌图标/中文版本信息/PerMonitorV2 DPI manifest → `go build -H windowsgui -s -w
   -X main.version=`。冒烟：标题栏带 v0.6.0、窗口/任务栏蓝圆图标、无控制台窗口、
   DPI 175% 正确、0804 中文版本信息可读。windowsgui 启动失败走 ui.AlertError 弹窗。
3. **README.md**：快速开始 / 防火墙（netsh 三条 + 端口表 TCP 9000 / UDP 50000-50100 /
   UDP 45921）/ 常见问题 / 参数表。
4. **回归**：capcheck 12/12 · codeccheck 14/14 · sigcheck 29/29 · uicheck 9/9
   （基线差 0.000%）· vet 全绿。

新约束（已写入 PLAN.md 51~53）：
- 51：Win32 位图 API 在 gdi32.dll 不在 user32；LazyProc 挂错 DLL 是 mustFind panic
  直接崩进程（崩在 goroutine 里，build/vet 全绿发现不了）——新 syscall 封装第一次必须真跑。
- 52：.ps1 必须纯 ASCII（PS 5.1 按 ANSI 读无 BOM 文件，中文字节吞引号直接炸解析）。
- 53：改完代码必须重新构建 dist 再验证（拿修复前的旧二进制复测，panic 行号对不上，
  白慌一场；验证前先核对"二进制构建时间 vs 最后改动时间"）。

另：本机副屏已断开（现在只枚举到 1 台显示器），跨屏类 e2e 暂时跑不了，注意。

残留待办（不在阶段 6）：单实例运行、开机自启、二维码分享。
