# GoShare — 内网局域网桌面共享客户端 · 实施计划

> 状态：**阶段 0（技术验证）已完成，方案已按实测结果修订**。
> 最后更新：2026-09-23 · 验证报告见 [`VERIFY.md`](VERIFY.md)
>
> ⚠️ **重大变更**：原定编解码方案 `go.264` H.264 未通过验证
> （Media Foundation 硬编在本机永久挂起；CPU 软编 1080p 仅 13.6fps）。
> **已更换为 Motion JPEG（标准库 + 条带并行）**，实测 2K 101fps / 1080p 206fps。
> 详见第 3.6 节与 VERIFY.md。

---

## 1. 目标与范围

单文件 Windows 客户端，双击即用。打开后两个主按钮：

- **我要分享** — 选择屏幕/区域 → 生成 `IP:端口` + 授权码 → 一键复制发给同事
- **我要观看** — 输入 `IP:端口` + 授权码 → 看到对方桌面

硬指标：内网端到端延迟 **< 100 ms**，支持多人同时观看，可配置共享质量，界面美观。

### v1 范围（已确认）

| 包含 | 不包含（后续） |
|---|---|
| 分享 / 观看双模式 | 网页观看端（v2，成本低） |
| IP:端口 + 授权码 + 一键复制 | 系统声音共享（WASAPI loopback） |
| 多网卡地址列表与复制 | 远程控制（风险高，慎做） |
| 局域网自动发现 | 录制 / 水印 |
| 暂停分享（黑屏保活） | macOS / Linux 支持 |
| 质量配置 + 诊断 HUD | |

---

## 2. 实测环境约束（2026-09-23 实测，非推测）

| 项 | 实测值 | 影响 |
|---|---|---|
| OS | Windows 10 企业版 LTSC **build 17763** | `WDA_EXCLUDEFROMCAPTURE` 需 19041+，**不可用** |
| 浏览器 | **无 Edge / 无 Chrome / 无 WebView2 Runtime**，仅 Firefox | Wails、Edge `--app` 方案全部排除 |
| 编译器 | **无 gcc / 无 clang** | CGO 不可用 → Fyne、Wails、webview_go 排除 |
| CPU / GPU | i7-13700H + Intel Iris Xe | Media Foundation 硬件 H264 编码可用 |
| 分辨率 | 2880×1800（高 DPI） | 必须做 DPI 感知 |
| 网卡 | 12 个 IPv4：以太网 `192.168.250.180`（默认路由）、ZeroTier ×2、VMware ×2、Hyper-V vEthernet、4× 169.254 APIPA | 多网卡过滤是刚需；ZeroTier 可异地直连 |
| 防火墙 | **Domain profile 已启用** | 入站可能被拦 |
| 目标机器 | 同事多为 Win10 新版 / Win11 | 有 Edge，网页观看端 v2 可补 |

---

## 3. 技术选型

### 3.1 已锁定

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | Go 1.26.3，`CGO_ENABLED=0` | 单二进制分发，无运行时依赖 |
| 屏幕采集 | `github.com/go-mswin/screencapture` | 纯 Go 无 CGO；DXGI Duplication + GDI 双后端；热路径零拷贝（16 ns/op，0 allocs）。**阶段 0 实测通过**：2K 采集 4.83ms，30fps 稳定，DPI 正确 |
| ~~编码 / 解码~~ | ~~`github.com/oops1/go.264` (v1.11.0)~~ | ❌ **已弃用**。MF 硬编挂起（三种绕过均失败）、CPU 软编 1080p 仅 13.6fps，见 VERIFY.md 第 2 节 |
| **编码 / 解码** | **Motion JPEG（`image/jpeg` 标准库 + 水平条带并行）** | **零外部依赖、零 cgo**。实测 2K 编码 9.9ms（101fps）、解码 4.8ms；1080p 编码 4.9ms、解码 2.8ms。每帧独立 → 天然秒开、丢帧不花屏、分辨率随意切换。详见 3.6 节 |
| 传输 | `github.com/pion/webrtc/v4` | ICE 打洞 + UDP 直连 + DTLS + NACK + 拥塞控制；内网无需 STUN/TURN。**阶段 0 实测**：建连 533ms，单向延迟 1.71ms，零丢包，吞吐 162.9 Mbps |
| UI | **Gio** | **阶段 0 实测通过**：`CGO_ENABLED=0` 构建并渲染成功，Win32 断言窗口真实可见。**已锁定 Gio，不需要回退 walk** |

### 3.2 采集库的三个已知缺口（必须自己补）

1. **不支持区域裁剪** — 只给整屏或单窗口（GDI），框选区域需自己按 `Stride` 裁切（stride ≠ width×4）
2. **DXGI 后端不合成鼠标光标** — 只给独立元数据，需自行叠加绘制
3. **多显示器 / 4K / GPU 路径作者标注"未证明"** — 必须在阶段 0 验证

### 3.3 关于"帧缓存"的修正（随编码器变更再次修正）

原始设想是"帧临时存内存，观众访问时一直读这块内存"。**改为推模型**：一次采集 → 一次编码 → 扇出 N 路，每路独立发送队列与拥塞控制。

> **改用 Motion JPEG 后，"GOP 环形缓存"整个不再需要。**
> 原先设计它是为了绕开"MF 硬编不支持 `ForceKeyFrame`、新观众要等一个 GOP"的限制。
> MJPEG 每帧自带完整画面，**新观众接入即出画（0ms），断线重连同理**。
>
> 缓存的用途改为**保留最近 1 帧**即可：新观众连上时立刻推这一帧，
> 保证"连上就有画面"，而不是等下一个采集周期（静态桌面下可能很久不出帧）。

### 3.4 分辨率与显示适配（跨设备清晰度）

**问题**：分享端 2880×1800（16:10）共享给 1920×1080（16:9）的同事，文字会糊甚至画面变形。这不是单一问题，而是四个叠加问题：

| # | 子问题 | 后果 |
|---|---|---|
| 1 | **宽高比不匹配** | 16:10 拉伸到 16:9 → 圆变椭圆、字体变形 |
| 2 | **降采样糊文字** | 2K 缩到 1K，非整数倍缩放破坏抗锯齿，笔画断裂 |
| 3 | **DPI 物理/逻辑像素混淆** | 分享端 2K @150% 缩放的逻辑分辨率是 1920×1200，与观看端对不上 |
| 4 | **降采样不可逆** | 发送端一旦降分辨率，观看端再放大就永久丢失细节 |

#### 实测数据（2026-09-23，i7-13700H 20 线程，`CGO_ENABLED=0`，复现代码见 `spike/scalebench`）

源 2880×1800：

| 操作 | 单线程 | 并行(20 workers) |
|---|---|---|
| BGRA→I420 2880×1800 | 26.14 ms | **4.29 ms** |
| I420 box 2:1 → 1440×900 | 4.88 ms | **1.10 ms** |
| I420 bilinear → 1728×1080 | 9.94 ms | **2.05 ms** |
| BGRA box 2:1 → 1440×900 | — | 1.79 ms |

**RGBA 域缩放（x/image/draw）—— 完全不可用：**

| NearestNeighbor | ApproxBiLinear | BiLinear | CatmullRom |
|---|---|---|---|
| 63 ms | 110 ms | 174 ms | 259 ms |

**完整流水线（并行，仅含转换+缩放）：**

| 方案 | 耗时 | 评价 |
|---|---|---|
| [A] 原生直发 2K | **4.21 ms** | 信息最全，且最便宜 |
| [B] I420 全量 → box 2:1 | 5.16 ms | |
| [C] BGRA box 2:1 → I420 | **3.26 ms** | 降分辨率时最快 |
| [D] I420 全量 → bilinear 1728×1080 | 5.65 ms | 非整数倍 |

**三条硬结论：**

1. **必须并行化** —— BGRA→I420 单线程 26 ms 直接超预算，并行后 4.3 ms（6× 加速）
2. **必须在 I420 域缩放** —— 比 RGBA 域快 20~60 倍；`x/image/draw` 在 2K 上不可用
3. **转换+缩放只占 4~6 ms** —— 编码才是大头，因此**默认有能力直发原生分辨率**

#### 四层适配策略

**层 1 · 宽高比（必须做，否则变形）**
- 一律 **contain**：保持源比例，多余部分留黑边，禁止拉伸填充
- 例：2880×1800(16:10) 投到 1920×1080 窗口 → 实际画面 1728×1080，左右各 96px 黑边
- UI 可选"拉伸填充"，但**不设为默认**

**层 2 · 发送分辨率决策（清晰度核心）**

| 档位 | 策略 | 适用 |
|---|---|---|
| **清晰优先（默认）** | 发送原生分辨率，观看端高质量缩放 | 看代码/文档/表格；实测仅 4.21 ms，付得起 |
| **均衡** | 按**网络带宽预算 + 编码耗时**自动下调（**与观看端窗口无关**） | WiFi、人数多 |
| **流畅优先** | 固定 1080p / 720p 档，走 [C] 路径 3.26 ms | 低配机、弱网 |

> **关键判断一：降采样不可逆。** 只要带宽和 CPU 允许，**优先发原生**，把缩放决策留给观看端——观看端可以放大看局部，发送端降了就再也拿不回来。

> **关键判断二（重要设计决策）：发送分辨率与观看端窗口大小必须解耦。**
> 早期设想"按观看端窗口 ×1.5 决定发送分辨率"是**错误**的：拖窗口是高频操作，一旦驱动发送分辨率，就会反复触发**编码器重建**（50~200 ms + 重发 IDR），拖动过程卡顿、CPU 抖动，迟滞参数还很不好调。
> **正确做法**：发送分辨率只由「主持人设定 + 网络带宽 + 编码耗时」决定；观看端窗口变化只影响**本地 GPU 缩放**（近乎零成本）。
> 唯一例外：观看端主动点"省流量/降画质"才请求降档。

**层 3 · 观看端渲染**
- 缩放交给 GPU（Gio/OpenGL shader），比 CPU 快且质量可控；默认双线性，文字模式可用 Lanczos
- **1:1 原始尺寸 + 平移**：窗口放不下时不缩放，显示原始像素、可拖动查看 —— **看代码的刚需**
- **区域放大**：框选局部放大到 200% / 400%
- 显示模式：适应窗口（默认）/ 1:1 / 自定义缩放

**层 4 · DPI 感知**
- 采集时记录 DPI 缩放比（如 150%），在信令里随分辨率一起下发
- 观看端按自身 DPI 换算，提供"按逻辑像素 1:1"选项（解决大字体用户对不上尺寸的问题）

#### 配套编码参数

文字内容对 H.264 的 4:2:0 色度抽样不友好（彩色文字边缘发糊）。清晰模式下应：提高码率、降 QP、**关闭 AQ**（go264 文档明确：自适应量化在滚动文本上比直接降 QP 差 1.8 dB）、短 GOP。

#### 动态协商（带迟滞）

观看端上报 **网络状况 + 显示 DPI**（**不上报窗口尺寸用于改分辨率**）。触发降档的唯一条件：
- 丢包率持续 > 2%，或
- 发送队列积压持续 > 1 s

且必须迟滞：**连续 3 秒满足条件**才调整，避免网络抖动导致反复重建编码器。

---

### 3.5 观看端窗口模式与"切屏"

#### 结论：默认窗口化 + 一键全屏，**不默认全屏**

| 方案 | 优点 | 缺点 | 采用 |
|---|---|---|---|
| 固定尺寸窗口 | 可预测、省资源 | 源为 2K 时看不清，须手动放大 | ✗ |
| 默认全屏 | 显示面积最大、沉浸感强 | 遮挡其他窗口，"边看边干活"被打断，退出需额外操作 | ✗ |
| **默认自适应窗口 + 一键全屏** | 兼顾专心看与边看边干 | — | ✓ |

**理由**：观看场景并不单一——专心看演示适合全屏，边看边操作适合窗口。默认全屏太霸道（用户正在做的事被强行打断），而窗口化 + 一键全屏两者都不牺牲。

具体行为：
- **首次打开**：按源宽高比自动选尺寸 = `min(源 × 0.6, 当前屏工作区 × 0.8)`，避免黑边浪费
- **三种显示模式**一键切换：
  - **适应窗口**（默认）— contain 缩放，保持比例
  - **1:1 原始尺寸** — 不缩放、显示原始像素、可平移（**看代码刚需**）
  - **全屏** — F11 / 双击标题栏 / 按钮；全屏**仍按比例留黑边，绝不拉伸**
- **记忆窗口位置、尺寸与模式**（跨会话持久化）

#### 拖窗口大小的影响（逐项拆解）

| 事件 | 对分享端 | 对观看端 | 处理 |
|---|---|---|---|
| 拖窗口大小 | **无影响**（已解耦） | 本地 GPU 缩放 | shader 双线性，Iris Xe 上 <1 ms |
| 全屏 / 退出全屏 | 无影响 | 重建 render target | 仍按比例 contain |
| 拖到另一台显示器 | 无影响 | **跨 DPI 需处理** | 响应 `WM_DPICHANGED`，按新 DPI 重算缩放，否则画面发糊 |
| 窗口最小化 | 无影响 | 暂停渲染省 CPU/GPU | 恢复时直接显示最新帧（GOP 缓存） |

#### "切屏"的两种含义都要满足

**A. 观看端切屏（窗口层面）**
- 可拖到任意显示器；支持"在**指定显示器**全屏"（如投到投影仪）
- 支持**窗口置顶**（画中画小窗，边干活边看）

**B. 分享端切换所共享的屏幕 / 区域（更关键）**

| 切换类型 | 处理 | 中断时长 |
|---|---|---|
| 切换共享**区域**（输出分辨率不变） | 仅改裁切矩形，**编码器无需重建** | **无感** |
| 切换**显示器**且输出分辨率相同 | 仅重建采集，编码器复用 | 约 0.2 s |
| 切换**显示器**且分辨率改变 | 停采集 → 重建采集 + 编码器 → 发新 SPS/PPS + IDR | 约 0.5~1 s |

**观看端必须能承受流中途的分辨率变化**：检测到 **SPS 变化** → 重建解码器 → 继续渲染，不能崩。go264 的 `Decoder` 是否支持原地重配置需实测（见 S0-8），否则走 `Close()` + `NewDecoder()`。

---

### 3.6 编码器：Motion JPEG（阶段 0 实测后变更，核心决策）

#### 为什么换

| 方案 | 2K 2880×1800 | 1080p | 判定 |
|---|---|---|---|
| H.264（go264 · MF 硬编） | — | — | ❌ `Encode()` 永久挂起（LockOSThread / CoInitializeEx 均无效） |
| H.264（go264 · CPU，20 slices） | 157.2ms → **6.4fps** | 77.6ms → **12.9fps** | ❌ 不达标 |
| **MJPEG（标准库 · 20 条带并行）** | **9.9ms → 101fps** | **4.9ms → 206fps** | ✅ |

**判断依据**：内网千兆场景下**带宽不是瓶颈，延迟和 CPU 才是**。MJPEG 恰好命中，且零依赖。

#### 实测数据（含 BGRA→I420 转换，i7-13700H 20 线程，最难的高频文本画面）

**编码**（条带数 → 耗时）

| 分辨率 | 1 条带 | 4 | 8 | 16 | **20** |
|---|---|---|---|---|---|
| 2880×1800 q75 | 91.9ms | 28.6 | 18.7 | 12.4 | **9.9ms（101fps）** |
| 1920×1080 q75 | 39.9ms | 12.9 | 9.3 | 6.1 | **4.9ms（206fps）** |

**解码（观看端）**

| 分辨率 | 1 条带 | 8 | **20** |
|---|---|---|---|
| 2880×1800 | 64.2ms | 11.2 | **4.8ms（210fps）** |
| 1920×1080 | 25.8ms | 4.2 | **2.8ms（364fps）** |

标准库 `image/jpeg` 是单线程的，**按水平条带并行后提速约 8 倍**——这是方案可行的关键。

#### 带宽代价与对策

| 分辨率 | 帧大小 | Mbps@30fps |
|---|---|---|
| 2880×1800 q75 | 1.71MB | 410 |
| 1920×1080 q75 | 680KB | 165 |

千兆内网可承载，但 2K 占 41%。**对策（按优先级）**：

1. **Dirty tile 增量更新（v1 必做）** —— 屏幕大部分时间只有小区域在动（鼠标、光标、输入）。
   按条带比对，只编码 + 发送变化的条带 → CPU 与带宽再降数倍。**这是 MJPEG 独有的优势，H.264 做不到这么细**。
2. **质量档位**：清晰 q85 / 均衡 q75 / 流畅 q65，或降输出分辨率。
3. **静止降帧率**：DXGI 变化驱动，桌面不动就不发包。

#### 带来的连锁简化

| H.264 时代的难题 | MJPEG 下 |
|---|---|
| 硬编不支持强制关键帧，需 GOP 缓存才能秒开 | 每帧独立，**天然秒开** |
| 丢 P 帧会花屏、需 IDR 恢复 | 丢帧只影响该帧，不累积 |
| 切分辨率要检测 SPS、重建解码器 | 天然支持 |
| B/P 帧重排序增加延迟 | 无重排序 |
| 依赖 go264（新库 + MF 死锁） | **零外部依赖** |

#### 帧格式（自定义，走 DataChannel）

```
[frameID:8][tileCount:2][tileIdx:2][tileY0:4][tileBytes:4][JPEG payload...]
```

观看端按 `tileIdx` 并发解码，按 `tileY0` 拼回整帧。条带高度取 16 的倍数（4:2:0 的 MCU 高度）。

---

## 4. 架构

```
[信令面 TCP]  观看端 --IP:端口 + 授权码--> 分享端 --校验--> 交换 SDP --> 建立 P2P

[媒体面 UDP]  采集(DXGI Duplication, BGRA)   ← 4.8ms @2K
                 ↓ BGRA→I420（并行）
              MJPEG 编码（水平条带并行）      ← 9.9ms @2K
                 ↓ dirty tile：只发变化条带
              最近 1 帧缓存（新观众秒开）
                 ↓ 扇出 ×N（每路独立队列）
              DataChannel(unordered, maxRetransmits=0)
                 over DTLS/SCTP/UDP · ICE 直连   ← 1.7ms
                 ↓
              条带并发 JPEG 解码                ← 4.8ms @2K
                 ↓ 拼接 → GPU 缩放 → 渲染 → 诊断 HUD
```

**实测端到端延迟**（各段相加，2026-09-23）

| 分辨率 | 采集 | 转换+编码 | 网络 | 解码 | 渲染(估) | **合计** |
|---|---|---|---|---|---|---|
| 2K 2880×1800 | 4.8ms | 9.9ms | 1.7ms | 4.8ms | ~3ms | **≈ 24ms** |
| 1080p | 3.0ms | 4.9ms | 1.7ms | 2.8ms | ~3ms | **≈ 15ms** |

加 jitter buffer 与呈现开销后仍在 **30~50ms**，远优于原定 <100ms 目标（腾讯会议通常 200~400ms）。

---

## 5. 风险与必做项清单

### P0 — 不做就会出事

| # | 项 | 说明 |
|---|---|---|
| 1 | **防自摄入** | LTSC 17763 无 `WDA_EXCLUDEFROMCAPTURE`，自己的窗口会被自己采集成无限套娃。方案：点"开始分享"后主窗自动最小化到托盘 |
| 2 | **DPI 感知** | 不设 DPI awareness，2880×1800 屏采图会错位、发虚 |
| 3 | **光标叠加** | DXGI 不合成鼠标，观众看不到操作位置。⚠️ **实测约束**：库的 `ShowsCursor` 会强制回退 GDI，采集耗时 4.8ms→**58.3ms**（12 倍）、掉到 17fps → **必须自己绘制光标，禁用 `ShowsCursor`** |
| 4 | **静止心跳** | DXGI 变化驱动，桌面不动就不出帧，观众侧误判断线 |
| 5 | ~~**首帧 IDR**~~ | ✅ **改用 MJPEG 后天然解决**：每帧自带完整画面，新观众接入即出画（0ms），不再需要 GOP 缓存与强制关键帧 |
| 6 | **防火墙** | Domain profile 开启；内置 `netsh` 放行指引 + 连通性自诊断 |
| 7 | **多网卡过滤** | 过滤 169.254 / 127 / 已断开，按默认路由排序；ZeroTier 单列"异地可达" |
| 8 | **授权码防护** | 失败次数限制、可轮换、观众列表可踢人、人数上限 |
| 9 | **端口冲突** | 9000 被占自动顺延，界面同步刷新 |
| 10 | **安全桌面 / UAC** | 触发 `ErrAccessLost` 后自动重建采集并重发 IDR |
| 11 | **宽高比 contain** | 16:10 → 16:9 直接拉伸会让圆变椭圆、字体变形；必须保持比例 + 黑边 |
| 12 | **并行化转换与缩放** | 单线程 BGRA→I420 达 26 ms 直接超预算；并行后 4.3 ms。缩放必须在 I420 域做 |
| 13 | **缩放模式可切换** | 仅"适应窗口"不够，看代码会因缩放糊到看不清；至少要有 1:1 原始尺寸 + 平移 |
| 14 | **窗口大小与发送分辨率解耦** | 拖窗口是高频操作，若驱动编码器重建会卡顿；解耦后仅本地 GPU 缩放，零成本 |
| 15 | **跨 DPI 拖动** | 窗口拖到不同 DPI 的显示器须响应 `WM_DPICHANGED`，否则画面发糊 |
| 16 | ~~**流中途分辨率变化**~~ | ✅ **阶段 0 已验证**：go264 `Decoder` 支持原地重配置（8 帧 1080p→8 帧 720p 全部正确）；改用 MJPEG 后本就天然支持 |

### P1 — 强烈建议

局域网自动发现（UDP 广播）· 一键复制整行 `192.168.250.180:9000 · 834915`（+ 二维码）· 暂停分享黑屏 · 分享前缩略图预览 · 质量预设（流畅/均衡/清晰）+ 高级项（缩放/帧率/码率/GOP）· 观看端全屏与自适应 · 断线自动重连 · UI 显示当前编码后端（`Backend()`: mediafoundation / cpu）· 单实例 + 托盘常驻 · 诊断日志一键导出

**分辨率相关：** 区域放大（框选局部 200%/400%）· DPI 缩放比随流下发 + "按逻辑像素 1:1" 选项 · 多档位归并（不同观看者需求归并到 ≤2 档，避免 N 个编码器）· 降档迟滞（丢包 >2% 或队列积压持续 3s 才触发）

**窗口相关：** 记忆窗口位置/尺寸/显示模式 · 指定显示器全屏（投投影仪）· 窗口置顶小窗（画中画）· 分享端显示器/区域热切换（无感或 0.2~1s 中断，见 3.5-B）

### P2 — 后续

网页观看端（v2）· 系统声音 · 录制 / 水印 · 远程控制

---

## 6. 阶段计划

### 阶段 0：技术验证（必须先完成，否则不往下走）

**阶段 0 已完成**（2026-09-23），完整数据与复现代码见 [`VERIFY.md`](VERIFY.md)。

| # | Spike | 通过标准 | 实测结果 | 判定 |
|---|---|---|---|---|
| S0-1 | 采集：枚举、DXGI 出帧、双屏、DPI、帧率 | 1080p+ @30fps 不偏移 | Backend=duplication；2K **4.83ms**/30.7fps；DPI 正确；双屏正确 | ✅ |
| S0-2 | go264：`Backend()`、1080p30 编码耗时 | 单帧 <10ms | MF 硬编 **永久挂起**；CPU 1080p **77.6ms**（12.9fps） | ❌ 换方案 |
| S0-3 | BGRA→I420 转换与缩放 | 1080p 单帧 <6ms | 2K 全流程 **4.21ms** | ✅ |
| S0-4 | pion：本机 P2P 直连与延迟 | 建连 <1s，延迟 <150ms | 建连 **533ms**，单向 **1.71ms**，零丢包，162.9 Mbps | ✅ |
| S0-5 | Gio：`CGO_ENABLED=0` 构建与渲染 | 能构建出可运行窗口 | 构建通过，渲染 61 帧，`IsWindowVisible=1` | ✅ 锁定 Gio |
| S0-6 | 2K 编码耗时 | <20ms/帧 | H.264 **157ms** ❌；**MJPEG 9.9ms** ✅ | ⚠️ 换 MJPEG |
| S0-7 | 解码耗时与清晰度 | 1080p 文字可辨认 | go264 38.1ms/帧；**MJPEG 2.8ms（1080p）/ 4.8ms（2K）** | ✅ |
| S0-8 | 流中途分辨率变化 | 1s 内恢复不崩 | go264 支持原地重配置（8 帧 1080p→8 帧 720p 全对）；MJPEG 天然支持 | ✅ |

**产出**：`VERIFY.md`（数字 + 结论 + 选型锁定）+ `spike/` 下 7 个可复跑的验证程序。

**遗留**：向 go264 作者反馈 MF 编码死锁（留一条未来回退 H.264 的路）。

### 阶段 1：采集与预览 ✅（2026-09-23 完成，受锁屏限制）

| 项 | 实现方式 | 实测结果 |
|---|---|---|
| 显示器枚举 | `capture.Displays()`，主屏优先排序 | ✅ 双屏正确，含 DPI/物理/逻辑像素 |
| 区域裁切 | `cropBGRA` 按 `Stride` 索引 + 并行 | ✅ 与整帧逐像素比对 **0 不一致**（800×600@200,100） |
| 光标叠加 | Win32 `GetCursorInfo`/`GetIconInfo`/`GetDIBits` 自绘，带句柄缓存 | ✅ 落点与**系统 `DrawIconEx` 绘制完全一致**（包围盒偏差 0） |
| DPI 感知 | `EnsureDPIAware()` 设 per-monitor aware | ✅ 0 → 2；修复前光标位置会差 1.75 倍 |
| 本地预览窗 | Gio + `widget.Image{Contain}` + 诊断 HUD | ✅ `CGO_ENABLED=0` 构建，渲染 187 帧，`IsWindowVisible=1` |
| 热切换区域 | `Source.SetRegion()` | ✅ 生效且不重建后端（无感切换前提） |
| 最近帧缓存 | `Source.Last()` | ✅ 静止时仍可提供兜底帧（P0-4 心跳） |

复现：`go run ./cmd/capcheck`（自检，9 项）、`go run ./cmd/goshare -exit 8s -log goshare.log`（预览窗）

⚠️ **本次采集性能数据不可信**：验证时机器处于**锁屏界面**，DXGI Duplication 被拒绝
（`E_ACCESSDENIED`），全部回退 GDI（38.9 ms / 21 fps）。
**解锁后必须重跑 `capcheck`** 才能得到真实性能（阶段 0 实测 DXGI 为 4.83 ms / 30.7 fps）。

未做（留给阶段 4 完整界面）：鼠标拖拽式的**交互式区域框选**。API 层已就绪（`SetRegion` 验证通过）。

### 阶段 2：编码与传输 ✅（2026-09-24 完成）

MJPEG 条带编码 · dirty tile 增量 · pion 双通道扇出 · 帧率档位 · 端到端演示
→ 产出：`cmd/codeccheck`（14/14 通过）、`cmd/netcheck`（16/16 通过）、`cmd/sharedemo`（真实桌面可见）

**实测（真实桌面，采集 1920×1080 副屏，i7-13700H / Iris Xe / CGO_ENABLED=0）**

| 档位 | 接收 fps | 带宽 | 编码 | 解码 | 采集处理 |
|---|---|---|---|---|---|
| **最大（默认）** | 29.6 | **8 Mbps** | 6.7 ms | 1.6 ms | 0.47 ms |
| 急速 | 29.5 | **5 Mbps** | 5.1 ms | 2.1 ms | 0.47 ms |
| 流畅60 | 29.6 | 9 Mbps | 6.2 ms | 3.4 ms | 0.41 ms |
| 流畅30 | 28.9 | 10 Mbps | 6.6 ms | 3.8 ms | 0.42 ms |

**合成高频内容（压力上限，`cmd/netcheck`）**

| 用例 | 编码+解码 | 端到端延迟 | 带宽 | PSNR |
|---|---|---|---|---|
| 1080p 全量 | 7.7 + 4.8 ms | 29.6 ms | 98 Mbps | 33.5 dB |
| 2K 全量（2MB/帧 → 21 分片） | 13.0 + 7.5 ms | 48.6 ms | 199 Mbps | 35.0 dB |
| **1080p 增量** | 7.4 + 3.6 ms | **17.5 ms** | **33 Mbps** | 33.7 dB |

> **关键结论**：真实桌面的带宽只有 5~10 Mbps，远低于阶段 0 按"全量"估算的 410 Mbps。
> 原因是真实桌面大部分区域静止，dirty tile 只编码变化的条带。
> 这直接解掉了"MJPEG 带宽太高"这个原本最大的顾虑。

**新增能力**：`internal/codec`（条带并行 MJPEG + 脏检测 + 线格式）、`internal/rtc`（media 不可靠无序 / ctl 可靠有序 + 60KB 分片重组 + 每路背压）、`internal/pipeline`（采集→编码→扇出，帧率档位、静止心跳、新观众触发全量帧）

### 阶段 3：信令与接入
HTTP 信令 · 授权码 · 多网卡列表 · 一键复制 · 局域网自动发现
→ 产出：同事机器能连上看到画面

### 阶段 4：界面与质量
双主按钮布局 · 质量配置 · 诊断 HUD · 暂停分享
→ 产出：界面可用且美观

### 阶段 5：健壮性
P0 清单全部落地 · 3~5 人同时观看压测 · 异常场景（锁屏/UAC/换屏/断网重连）
→ 产出：压测报告

**进行中（2026-09-24）** —— 已完成的：
- [x] 补做阶段 4 遗留的**画面内容级端到端复验**（桌面已解锁）：观看端收到 **非黑 98.2% / 亮度 209**，
      与采集源同刻读数（208.1）一致；首帧即完整画面
- [x] **修复 R32：观众接入缺全量帧导致大片黑屏** —— 见风险表。
      三条恢复路径（通道就绪后请求 / 接入窗口密集补 / 观众端 PLI）已**分别**做过反向变异验证，
      反向变异下自愈时间从 **12 秒 → 1 帧（~33ms）**
- [x] 消除"接入必丢全量帧"的时序竞态，并把全量帧开销限制在接入窗口内
      （稳态全量帧计数停止增长，带宽无固定额外开销）
- [x] 顺带修：HUD 的"编码 X ms"—— `Stats.EncodeMs` 已是均值，此前被误改成再除一次帧数（显示 0.0）
- [x] 分享端补上每 3 秒一条的运行统计日志（fps / 带宽 / 编码均值 / **全量帧占比** / 观众数），
      带宽异常时先看全量帧占比

- [x] **多人同时观看压测**（新工具 `spike/multiview`：无窗口 N 观众，各自独立统计）—— 见 VERIFY 第 14 节。
      3 人 / 5 人 / 上限争抢 / 名额释放 / 强杀回收五组，关键读数：
      - 3 人：3/3 拿到完整画面，**帧数极差 0.1%**，每人 2.6~2.9 Mbps，28~29 fps
      - 5 人：5/5 **首帧即完整**，编码均值 0 人 4.5 ms → 5 人 5.3 ms（**几乎不随人数增长**）
      - 中途踢掉 1 人：其余 4 人 12 秒内各多收 357 帧，**完全一致，无影响面**
      - 上限 `-max 2` + 4 人同时抢：2 进 2 拒（"观看人数已满"，拒绝干净）
- [x] **观众名额回收三种断开方式**（幽灵观众回归）：
      正常 Bye → 立刻释放；关 Peer → `closed` 立刻释放；**进程被 taskkill 强杀 → `disconnected` → 5s 后释放**；
      三种释放后重试补位均成功（补位观众首帧即完整）

**待办**：
- [ ] 异常场景：采集源中断（锁屏/安全桌面）自动重建、换屏/换区域
      ⚠️ 锁屏会锁住真实桌面且我无法自行解锁，需人工确认；先看 `capture` 层 `AccessLost` 重建路径的代码
- [ ] 观众"断网重连"已部分覆盖（重试补位验证过），完整链路（ICE 重建后画面自愈）待做
- [x] **P0 #1 防自摄入：已实现**（`internal/ui/winmin_windows.go`）。
      分享开始后把主窗最小化（`ShowWindow(SW_MINIMIZE)`），并顺手设 `WDA_MONITOR` 兜底 ——
      万一用户又把窗口还原，采集里也是一块黑矩形，仍远好过套娃。
      **实测效果**（同为主屏采集）：全量帧从"9→17 持续增长"变成"**停在 5，460 帧内不再增长**"，
      带宽 82 Mbps 持续 → **13 → 0.8 Mbps**（降到静止）。
      待办：真正的**托盘图标**（现在是任务栏最小化，恢复要点任务栏；托盘更利于后台常驻）
- [ ] P0 其余项逐项核对待补：见下方"P0 核对"小节
- [ ] 端口占用预检误判（约束 32）—— **复查结论：代码本身就是"直接 Listen，失败再顺延"
      （`signal/server.go` 里 `net.Listen` 失败才 `continue`），不是预检**；当时 9000 报占用大概率是
      Windows 保留端口/Hyper-V 动态端口段，`netstat` 看不到监听者。待办应改为：
      顺延日志补一句"可能是系统保留端口"，避免误判成"有残留进程"
- [ ] **HUD 的 Mbps 是单份编码码率，不是实际出网带宽**（N 个观众就是 N 份）。
      5 人时显示 4.0 Mbps 而出网约 16 Mbps，会误导；要么乘人数，要么改标"每路"
- [ ] **KeyBurst 窗口是全局的**：新观众接入会让**已接入的观众也陪着吃全量帧**
      （实测 #1 收到 39 个全量帧，同批 #3 只有 7 个）。接入期带宽会短暂升高，
      5 人依次接入时高带宽期约 15 秒。可优化：burst 只在新观众"确实缺画面"时才补

### 阶段 6：打包分发
单 exe · 托盘 · 使用说明 · 防火墙说明
→ 产出：可直接分发的包

---

## 7. 目录结构（草案）

```
Go_share_desktop/
├── cmd/goshare/          主程序入口
├── internal/
│   ├── capture/          采集封装：区域裁切、光标叠加、DPI、后端降级
│   ├── codec/            go264 封装、BGRA→I420、GOP 环形缓存
│   ├── rtc/              pion Peer、扇出、jitter、统计
│   ├── signal/           HTTP 信令、授权码、会话管理
│   ├── netif/            多网卡枚举 / 过滤 / 排序
│   ├── discover/         UDP 广播局域网发现
│   └── ui/               Gio（或 walk）界面
├── PLAN.md
└── go.mod
```

---

## 8. 风险登记

| ID | 风险 | 应对 |
|---|---|---|
| R1 | ~~采集库多显示器 / GPU 路径未验证~~ | ✅ **阶段 0 在真实 GPU（Iris Xe）+ 双屏上验证通过**：Backend=duplication，2K 4.83ms，30fps 稳定 |
| R2 | ~~go264 新库缺少验证~~ | ❌ **已证实不可用**：MF 硬编死锁、CPU 软编性能不足 → **已弃用 go264**，改用标准库 MJPEG。向作者反馈 MF 死锁以保留未来回退可能 |
| R2b | **MJPEG 带宽偏高**（2K 410Mbps @30fps） | Dirty tile 增量更新（v1 必做）+ 质量档位 + 静止降帧率；千兆内网当前已可承载 |
| R3 | MF 硬编不支持强制关键帧 | 短 GOP + GOP 缓存秒开 |
| R4 | Gio 在 Windows 无 CGO 构建可能失败 | 回退 walk（Tailscale fork） |
| R5 | LTSC 17763 无窗口排除 API | 最小化到托盘 + 可选"隐藏自身窗口区域" |
| R6 | Domain 防火墙拦截入站 | 内置放行指引 + 自诊断 |
| R7 | 高 DPI 采图错位 | 阶段 0 验证 DPI awareness |
| R8 | 多观看者分辨率需求不同 → N 个编码器 CPU 爆炸 | 档位归并到 ≤2 档；默认单档 + 观看端缩放 |
| R9 | 纯 Go 缩放在 2K 上过慢 | 已验证：仅在 I420 域做 + 并行化，控制在 6 ms 内 |
| R10 | 宽高比不匹配导致画面变形 | contain 强制保持比例，拉伸仅作可选项 |
| R11 | 拖动窗口频繁触发分辨率重协商 → 卡顿 | **架构上解耦**：窗口变化不改发送分辨率，仅本地 GPU 缩放 |
| R12 | ~~切屏导致观看端解码器失效~~ | ✅ **阶段 0 已验证** go264 支持原地重配置；MJPEG 本就天然支持 |
| R13 | 观看端跨 DPI 显示器拖动画面发糊 | 响应 `WM_DPICHANGED` 重算缩放；阶段 1 验证 |
| **R14** | **锁屏 / UAC 安全桌面 / RDP 断开时 DXGI 被拒** | 实测：`DuplicateOutput` 返回 `E_ACCESSDENIED`，静默回退 GDI（38.9ms/21fps）。库会自动回退不会报错，但性能差 8 倍 → **必须在 HUD 显式显示后端与降级原因**，并在降级时提示用户；触发 `ErrAccessLost` 后需能自动重建 |
| **R15** | **进程非 DPI aware 导致光标位置差一个 scale** | 高 DPI 屏上 `GetCursorPos` 返回逻辑像素，与物理像素帧差 1.75 倍。**危险之处：错误完全自洽**——自绘位置与 `GetCursorPos` 一致，看不出画错，必须靠「系统自己画的光标」作独立真值才能发现。已用 `EnsureDPIAware()` 修复 |
| **R16** | Gio 关窗后进程不自行退出 | 实测：`Perform(ActionClose)` 后渲染停止但 `app.Main()` 不返回，留下无意义退出码 `0xCFFFFFFF`。自动化场景需兜底 `os.Exit` |
| **R17** | 采集库 `ShowsCursor` 会强制回退 GDI | 采集 4.8ms → 58.3ms（12 倍）。**代码中已禁用该选项**，光标一律自绘 |
| **R18** | **外部 `SetWindowPos` 移动 Gio 窗口后不再出帧** | 实测：移动后停在第 10 帧，不移动可跑到 889 帧。→ **不要从外部移动 Gio 窗口**；演示改用「采集副屏、观看窗留主屏」规避自摄入 |
| **R19** | **RGB↔YUV 色彩范围两端不一致** | 编码写成 limited-range 而解码按 full-range 反变换 → 系统性偏色，PSNR 只有 23 dB，且「提高质量几乎无改善」就是这个 bug 的指纹。已统一为 BT.601 full-range，PSNR 恢复到 34.8 dB |
| **R20** | **解码画布被解码线程与渲染线程同时访问** | 解码器的画布是持久复用的，直接交给 Gio 渲染会撕裂甚至让后端崩溃。解决：画布池 + 渲染完归还 |
| **R21** | **限帧判定卡在边界会误丢帧** | 严格 `now-lastSend < interval` 判定时，30fps 档实际只发出 19.7fps。需留 3ms 容差，修复后 28.9fps |
| **R22** | **重组槽按数量清理会误删活跃帧** | 曾「超过 8 个就删一个」，导致莫名丢帧（2K 掉 1 帧、增量掉 2 帧）。不可靠无序通道下分片乱序，活跃帧数可能远超预期 → 只能按超时清理 |
| **R23** | **别用随机噪声模拟桌面变化** | JPEG 对逐像素噪声 PSNR 会崩到 20 dB 以下，看起来像编码坏了，实际是测试内容不真实。真实桌面变化是文本/窗口/色块这类结构化内容 |
| **R24** | **`paint.PaintOp` 不设 clip 会铺满整个窗口** | 观看端状态栏背景漏写 `clip`，面板色盖住先画好的视频画面 → **画面全黑、只剩状态栏文字**。Gio 的 `PaintOp` 填充的是"当前裁剪区域"，而 `layout.Rigid` 不会替你设裁剪。已修（`viewer.go` / `preview.go` 统一改为"先量高度、再按高度裁剪"），并用反向变异验证：去掉 clip 立刻复现全黑 |
| **R25** | **DXGI Duplication 建立后的首帧可能是未初始化的黑帧** | 实测 3 次里 2 次出现（两块显示器都有）。观众端开屏闪一帧黑；若桌面恰好静止，这帧黑会挂到下一次桌面变化。当前仅记录未修（第二帧起即正常），**阶段 3 接入多观众前需处理**（预热丢一帧 + 有限超时兜底，不能无条件丢首帧，否则静止桌面会永远等不到画面） |
| **R26** | **"渲染了 N 帧"证明不了"画面可见"** | 阶段 1/2 的验证只看 `hwnd visible` 与渲染帧计数，两者在画面被完全盖住时**依然通过**——R24 就是这么漏掉整个阶段的。凡涉及画面输出的改动，**必须用像素证据（截图 + 非黑占比统计）收口**。工具：`cmd/shot`（截屏存 PNG）、`cmd/pixprobe`(逐环节像素统计)、`cmd/viewertest`（隔离渲染层与数据层） |
| **R27** | **绝不能在 pion 的状态回调里同步做清理** | 观众退出时 `OnState(closed)` → 同步 `peer.Close()`：回调占着 ICE agent 的内部 goroutine，而 `Close` 要等 agent 收尾 —— 两边互等，**整个分享端当场静默卡死**（无 panic、无日志），后续所有观众接入都 `context deadline exceeded`。实测两次复现。正解：清理动作挪进独立 goroutine，回调立即返回。同理，**幂等不能用 `sync.Once`**（cleanup → srv.Drop → closeFn → 又是 cleanup，同 goroutine 重入 `once.Do` 永久阻塞），要用 mutex+flag |
| **R28** | **别持锁回调，也别造回调环** | `Server.Kick` 原本在 `s.mu` 里调 `v.closeFn()`，而 closeFn 会反向调 `srv.Detach/Drop` → 抢同一把锁自锁。规则：**先摘记录、解锁、再回调**。另外 `Drop`（移除+回调）和 `Detach`（只移除）必须分开：分享端 cleanup 里已经自己关了 Peer，就该用 `Detach`，否则形成 Drop→closeFn→cleanup 的回环 |
| **R29** | **锁屏时采集到的画面是全黑的** | 会话锁屏后 DXGI Duplication 取不到内容，静默回退 `gdi`，而 GDI 也抓不到安全桌面 —— 每帧都是黑的，观众看到的是一片黑且**带宽接近 0**。实测：非黑 **0.0%**、亮度 **0.0**、0.0 Mbps。已加自诊断：`Sharer.BlackScreen()` 抽样统计连续全黑帧（15 帧抽一次、连续 4 次判定），命中时 HUD 显示「采集到的画面是全黑的」并打一条日志（去重，只在状态翻转时报） |
| **R30** | **动态列表的按钮控件必须随状态扩展** | 地址列表的「复制」、观众列表的「断开」按钮存在 `[]widget.Clickable` 里。状态更新时忘了扩切片，新出现的行就**整列没有按钮**——界面上看是"设计如此"，实际是漏了。规则：`SetShareState` / `SetJoinState` 里同步 `append` 到够用（用离屏渲染校验才看出来） |
| **R31** | **"粗粒度像素统计通过"不等于"界面没坏"** | R30 这种"按钮整列消失"只改动 **2.3%~4.2%** 的像素，非黑占比、颜色数、亮部三项**几乎不动**（色数 1055→1057），`uicheck` 原来的判据（非黑>2% 且 色数>8）**照样 8/8 通过** —— 是逐个像素比对才照出来的。规则：界面校验必须有**基线逐像素比对**（`uicheck -golden testdata/ui-golden`，差异 >0.5% 判失败），粗统计只能当"完全没画出来"的兜底，不能当回归门禁 |
| **R32** | **全量帧只发一次，撞在通道未就绪上就永久丢失** | 观众接入时触发的那一次全量帧，实际编码发生在 `AddPeer` 之后约 33ms —— 那一刻 DataChannel 往往还在 DTLS 握手，`SendFrame` 直接 `ErrNotOpen` 丢掉；而编码器已把 `havePrev` 置真，**全量帧只此一次**。增量帧只覆盖"变化过的"条带，于是静止区域**永远是黑的**。实测：观众开局只拿到 **11.8%** 的画面，靠桌面恰好大幅变化才在 **12 秒 / 226 帧**后偶然补全，桌面不动就可能一直黑下去（用户报的"全黑看不见东西"）。修复＝三条**互相独立**的恢复路径：① `WaitReady` 后补一次请求；② 新观众接入后的补帧窗口内按 400ms 密集补全量；③ 观众端发现画面不完整（或丢了分片）就发 `CtlPLI` 走可靠 ctl 通道请求全量。三条已**分别**做过反向变异验证，仅剩 PLI 时 1 帧（~33ms）即可补全 |

---

## 9. 待办

### 已完成
- [x] ~~初始化 git 仓库~~ —— 工作区已是仓库（`main` + `origin/main`）
- [x] 阶段 0 技术验证 —— 8 个 spike 全部跑完，报告见 `VERIFY.md`，选型已锁定

### 进行中 / 下一步
- [x] 阶段 1：采集与预览 —— 2026-09-23 完成（自检 9 项通过，预览窗跑通）
- [x] **在解锁桌面下重跑 `go run ./cmd/capcheck`** —— 已复跑：后端恢复 `duplication`，2K 处理 1.20 ms
- [x] 阶段 2：编码与传输 —— 2026-09-24 完成（codeccheck 14/14、netcheck 16/16、真实桌面演示跑通）
- [x] Dirty tile 增量更新 —— 已实现并验证：真实桌面带宽从理论 200 Mbps 降到 **5~10 Mbps**
- [x] **修复观看窗全黑（R24）** —— 2026-09-24：状态栏背景 `PaintOp` 漏了 clip，铺满整窗盖住画面。
      已修 `viewer.go` + `preview.go`，反向变异验证通过；顺带补上像素级验证工具（`shot` / `pixprobe` / `viewertest`）
- [x] 阶段 3：信令与接入 —— 2026-09-24 完成：
      `internal/netif`（多网卡枚举，实测 6 个可分享地址、默认路由排首位）、
      `internal/signal`（HTTP 信令 + 授权码 + 失败限次冷却 + 观众上限 + non-trickle ICE）、
      `internal/discover`（UDP 广播自动发现）、`internal/clip`（一键复制，纯 syscall 无 CGO）、
      `cmd/sigdemo`（host/watch 两端）、`cmd/sigcheck`（信令自检 29/29）。
      实测：跨进程 + 局域网真实 IP 接入成功（握手 531~889ms，27~29fps / 8 Mbps）；
      两观众并发在线「当前 2 人」；指定地址与自动发现两条路径均可接入
- [x] 处理 R25：DXGI 首帧黑 —— 已修：`capture.Source.Warmup()` 预热丢弃未初始化黑帧
      （有限次数 3 + 600ms 超时兜底，且预热期不更新 `Last()`，避免静止桌面观众永远看黑）。
      实测 10/10 → 0/10；已固化进 `capcheck` 第 12 项
- [x] 修复 R27/R28：观众退出时分享端卡死 —— 见风险表
- [x] 阶段 4：界面与质量 —— 2026-09-24 完成：
      `internal/ui/shell.go`（单窗口 + 路由状态机）、`internal/ui/pages.go`（六个页面）、
      `cmd/goshare` 重写为双主按钮入口 + 完整业务编排。
      含：首页双主按钮 / 分享设置（选显示器、整屏或框选区域、四档质量）/
      分享中（本地回显、授权码与多地址一键复制、诊断 HUD、档位热切换、暂停恢复、
      观众列表与踢人、停止）/ 观看接入（地址+授权码、局域网发现列表、失败重试）/
      观看中（画面 + 状态条）/ 交互式区域框选（桌面快照 + 拖拽挖洞选区 + 实时尺寸）。
      新增 `cmd/uicheck`（离屏渲染校验，8/8 通过）+ **基线逐像素比对**
      （`testdata/ui-golden`，差异 >0.5% 判失败，可当回归门禁 —— R31）
- [x] 交互式区域框选 —— 已实现：抓桌面快照做背景，全屏窗口拖拽选区，
      四周压暗挖洞 + 蓝色边框 + 实时显示 `宽×高 @ (x,y)`，确认后热切换（`SetRegion`）
- [x] 暂停分享 —— 已实现：暂停期间每秒发一帧纯黑（观众能区分"被暂停"与"卡住"），
      恢复时强制全量帧避免 dirty tile 残影
- [x] 诊断 HUD —— 已实现：发送 fps / 带宽 / 编码耗时 / 采集后端 / 已发帧数 / 观众数 /
      共享范围 / 全黑告警
- [ ] 向 go264 作者反馈 MF 编码死锁（保留未来回退 H.264 的路）
- [ ] 验证 `Options.ExcludeWindows`（WDA_EXCLUDEFROMCAPTURE）在 17763 是否真不可用
- [ ] **画面内容级端到端复验**（下次解锁桌面时跑，见 VERIFY.md 第 12 节）：
      当前会话锁屏导致采集全黑，只能验证到"连接/帧率/UI/告警"，画面正确性待复跑

### 阶段 0 遗留的已知约束（写代码时勿忘）
1. **禁用 `Options.ShowsCursor`** —— 会强制回退 GDI，采集 4.8ms→58.3ms。光标必须自绘。
2. **索引帧数据时必须用 `Frame.Stride`**，不能假设 `Width*4`（本机恰好相等，换机器不一定）。
3. **性能测量必须用变化的帧** —— 喂同一帧会让编码器大量 skip，得出过于乐观的数字（本次踩过）。
4. 构建 `screencapture` 时需 `GOSUMDB=sum.golang.google.cn`（该模块要求 go>=1.26.4，会触发 toolchain 下载，`GOSUMDB=off` 会导致校验失败）。

### 阶段 1 新增的已知约束
5. **必须先调 `capture.EnsureDPIAware()`**，且要在枚举显示器、创建窗口之前。
   否则 `GetCursorPos` 返回逻辑像素，光标位置差一个 scale（本机 1.75 倍）。
6. **锁屏 / UAC 安全桌面 / RDP 断开时 DXGI 会被拒**并静默回退 GDI。
   性能数据只有在不锁屏时才有意义；产品里要把后端与降级原因显示给用户。
7. **验证光标落点必须用独立真值**：让系统用 `ShowsCursor=true` 画一次作为参照，
   不能只比对「自绘位置 vs `GetCursorPos`」——两者可能错得完全自洽。
8. **Gio 关窗后进程可能不退出**，自动化验证需兜底。
9. 包内 `cursorMu` 与共享 DC 的锁必须分开（`dc()` 会在持有 `cursorMu` 时被调用，
   用同一把锁会立刻自锁 —— 本次踩过，表现为进程静默挂起无输出）。

### 阶段 2 新增的已知约束
10. **状态栏/面板类背景必须显式裁剪**：`paint.PaintOp` 填充的是"当前裁剪区域"，
    `layout.Rigid` 不会替你设裁剪。漏一个 `clip`，背景色就会铺满整个窗口，
    把上面画好的视频画面整块盖掉 —— 症状是"画面全黑、只剩状态栏文字"（R24，本次踩过）。
    正确写法：`op.Record` 先量出内容高度 → `clip.Rect{Max: image.Pt(Max.X, 真实高度)}` 内画背景 → 播放内容。
11. **不要拿 `gtx.Constraints.Max` 当 Rigid 子项的高度**：它是整个窗口尺寸，不是状态栏高度。
12. **"画了没有" 和 "画了看不见" 必须分开验证**：`IsWindowVisible` + 渲染帧计数在画面被完全盖住时
    依然全部通过，R24 就这样穿过了整个阶段 2。凡涉及画面输出的改动，**用 `cmd/shot` 截图 +
    `cmd/pixprobe` 像素统计收口**，不要只信日志。
13. **DXGI 首帧可能是未初始化的黑帧**（实测 2/3 次出现，两块屏都有）。第二帧起正常。
    修的时候注意：不能无条件丢首帧，否则桌面静止时观众永远等不到画面（R25）。

### 阶段 3 新增的已知约束
14. **pion 的 ICE 状态回调里不要做任何会等待 ICE 的动作**。回调跑在 ICE agent 的内部
    goroutine 上，`peer.Close()` 要等 agent 收尾 → 互等死锁，分享端**静默卡死**
    （无 panic 无日志，只有后续接入超时能看出来）。清理必须 `go cleanup(...)`（R27）。
15. **幂等别用 `sync.Once`**：`cleanup → Server.Drop → closeFn → cleanup` 是同 goroutine
    重入，`once.Do` 会永久阻塞。用 `mu + bool flag`，重入直接 return（R27）。
16. **先摘记录、解锁、再回调**：`Server.Kick` 原来持 `s.mu` 调 closeFn，而 closeFn 会回头
    调 `srv.Detach` → 同一把锁自锁。`Drop`（移除+回调）与 `Detach`（只移除）语义要分开，
    分享端 cleanup 用 `Detach`（R28）。
17. **观众被强杀时没有 Bye**，只能靠 ICE `disconnected` 超时兜底（5s）。实测有效：
    强杀两个观众后分享端在 5s 内把它们回收成「剩余 0 人」。
18. **验证接入功能时，观众进程必须用 `run_in_background` 起**。用 `cmd &` 起的话，
    Bash 工具调用一返回子进程就被回收，会误判成"接入失败"或触发假的断线兜底路径。

### 阶段 4 新增的已知约束
19. **Gio v0.10 换了输入模型**：旧的 `pointer.InputOp{Tag:...}` 已废弃，改为
    `event.Op(ops, tag)` + `gtx.Event(pointer.Filter{Target: tag, Kinds: ...})` 配对使用。
    只写 `event.Op` 不调 `gtx.Event` 收不到任何事件；反过来没有 `event.Op` 则 `gtx.Event` 恒空。
    另外 `clip.Rect{}.Op()` **不带参数**（旧写法 `Op(ops)` 会报 too many arguments），
    `app.Size(...)` 本身已是 `Option`（再调 `.Option()` 报 undefined）。
20. **多页面切换用"单窗口 + 路由状态机"，不要开多个 `app.Window`**。
    Gio 一个 `app.Window` 就是一条事件循环，多窗口要么抢事件要么整窗不刷。
    本项目所有页面（首页/设置/分享中/加入/观看中/框选）共用 `ui.Shell` 的 `route` 字段切换。
21. **动态列表的按钮容器必须跟着状态长度增长**：`addrCopies` / `kickBtns` / `foundBtns`
    这类"每行一个按钮"的切片，在 `SetShareState` / `SetJoinState` 里要先 `append` 到 `len(state)`。
    否则新出现的行没有对应按钮 → **复制/踢人按钮直接不渲染**（R30，本次踩过，
    且离屏渲染之前一直没发现）。
22. **预设高亮要读业务实际值，不要读本地选择**：分享进行中应以 `share.PresetName`（业务回报）
    为准，否则 UI 上高亮的档位和实际生效的档位会不一致（`presetHighlight()`）。
23. **观众接入要有重试**：分享端可能还在 ICE gathering，观众一次拨号很容易 `deadline exceeded`。
    `joinAttempts=3` / `joinRetryWait=4s`；**确定性失败（授权码错 / 已满）必须提前 break**
    —— 重试只会让用户多等 8 秒还看到同一个错。失败要打日志，静默失败最难查。
24. **暂停分享要发"节奏性黑帧"而不是停发**：暂停时每秒发一帧纯黑，观众侧就能把
    "分享方暂停了"和"卡住了/断了"区分开。恢复时必须**强制全量帧**，
    否则 dirty tile 增量会把暂停前的残影留在观众屏幕上。
25. **全黑告警必须做"翻转才打日志"**，否则 HUD 每 500ms 刷一次就每 500ms 打一行，
    几秒钟就把日志刷爆（本次踩过：修之前 30 行，修之后 1 行）。
26. **锁屏时采集层拿到的就是真黑帧**：不是 bug，DXGI 被拒后回退 GDI 也拿不到内容。
    所以阶段 4 的自诊断（`BlackScreen()`）在锁屏下**必然报全黑**，这是预期行为，
    不能据此判断"诊断写错了"。
27. **界面类验证在锁屏下的唯一可靠通道是离屏渲染**（`cmd/uicheck`，`gpu/headless`）。
    桌面截图（`cmd/shot`）在锁屏时截到的是锁屏蓝底，像素证据无效；
    真窗口又依赖会话 Active。离屏渲染绕开了这两条限制 —— 但它只验证"UI 画对了"，
    **验证不了"画面内容对"**，两者不能互相替代。
28. **界面回归必须用基线逐像素比对，不能用粗统计**（R31）。离屏渲染是**确定性**的
    （同二进制连跑两次 0 像素差异），所以可以存基线 PNG 后逐像素比。
    `uicheck -golden out/golden` 差异 >0.5% 即判失败并退出码 1（可当门禁）；
    `-golden <dir> -save-golden` 写基线。界面有**故意**改动时用 `-save-golden` 更新基线，
    并说明改了什么 —— 别把"更新基线"当成"绕过失败"。基线存在 `testdata/ui-golden`
    （`out/` 被 .gitignore 忽略，不能当基线仓）。
29. **离屏渲染会偶发 `0x887a0005`（DXGI_ERROR_DEVICE_REMOVED）并 panic**，
    表现为首页全黑 + `ReadPixels` 失败 + GPU 设备丢失的 panic 栈。**这是偶发，不是界面坏了**：
    同一二进制紧接着重跑一次就 8/8 通过。看到这个错先重跑，别急着改代码；
    也别把"出现 panic"当成门禁失败。

### 阶段 5 新增的已知约束
30. **"只在某一刻发一次"的关键帧必须配一条反馈路径**（R32 的通用教训）。
    全量帧、IDR、重配置后的首帧都属于这类。只要它可能丢（通道未就绪 / 分片丢 / 背压），
    就不能假设那一次一定送达 —— 必须有"收不到就再要"的回路，否则症状是**大片静止区域永远黑**，
    而它看起来完全不像"丢了一个包"。最低要求两条：发送侧在通道就绪后补一次，
    接收侧发现不完整就主动请求。
31. **`pipeline.Stats.EncodeMs` 在 `Stats()` 里已经折算成均值**（`st.EncodeMs /= float64(st.Frames)`），
    拿到手就是"每帧编码耗时"，不要再除一次帧数 —— 除两次会稳定显示 `0.0 ms`。
    教训：结构体里 `+=` 累加、`Stats()` 里折算，是两处配合；只看累加那一行就断定"这是累计值"，
    会把本来正确的显示改错（本次踩过，改完还得改回来）。
32. **端口占用预检会误判，而它会静默污染整组验证数据**。实测 `-port 9000` 报"被占用，已顺延到 9001"，
    但那一刻 9000 上并没有监听者（残留进程已退，可能只是 TIME_WAIT）。
    危险不在端口号变了，而在：观看端仍按 9000 去连 → **连上了别的分享端或直接失败**，
    而日志里"握手成功"看不出任何异常。自动化验证里**必须回读 host 日志里"分享已开始：端口 N"
    的实际端口**再去连，不能假定端口就是命令行给的那个。
33. **本机验证环境会拦截 `rm`**：`rm -f e2e*.log` 被安全删除机制接管，trash 操作失败后
    整条 `&&` 命令被 FAIL_CLOSED 阻断 —— 症状是"分享端没起来"，极易误判成代码问题。
    清理日志别用 `rm`，换个文件名继续跑。
34. **不要把 `go build` 和"运行该产物"放进同一条后台命令**：后台执行时后续步骤可能先于
    build 完成而用到**旧二进制**，症状是"新加的日志一行都不打"（本次踩过，白白多跑一组实验）。
    规则：前台 build 完，再另起进程。
35. **跨时间的像素基准不可比**：同一块屏十几分钟内内容就会变（实测副屏平均亮度 37.5 → 208）。
    "采集源 vs 观众收到的画面"这类对照必须**同时刻**取样；拿十几分钟前的读数去比，
    会得出"采错显示器了"这种假结论（本次差点据此改错代码）。
36. **分享端在"没有观众"时也在持续编码**（`分享统计` 里 `观众 0 人` 仍有 2~11 Mbps）。
    目前无害（省下的 CPU 有限、逻辑最简单），但人数为 0 时按需启停是明确的优化项。
37. **⚠️ 从终端启动的控制台程序会自己弹窗，而这个窗口会进采集画面（自摄入），
    把整批压测数据污染掉 —— 本次差点据此得出完全错误的结论。**
    实测：分享端采主屏 + 从 bash 起观看端（Go 控制台程序），观看端每打印一行就滚一次屏 →
    画面持续剧变 → 触发全量帧 → 又打印一行，**自激振荡**。
    读数：观众 0 人时就 **84→108 Mbps、全量帧 9→186**；干净基线其实只有 **0.0~4.2 Mbps、全量帧 1/871**，
    差了 10 倍以上。修法（本次采用）：分享端 `-display 1` 采副屏，控制台窗口留在主屏，
    根本进不了画面；或让被启动的进程彻底不建窗口。
    判据：**"观众 0 人"时的带宽必须接近 0** —— 只要基线不是 0，先怀疑自摄入，别急着改编码器。
38. **公平性统计不能把"中途被踢掉的观众"算进极差**：他帧数天然少一截，
    会得到 50% 这种假数字，看着像严重不公平（实际在线的那几个极差只有 0.1%）。
39. **重试成功的观众，帧率/首帧延迟必须用"真正接通的时刻"当分母**：
    用第一次尝试的时刻当分母，会把 30 fps 摊薄成 3 fps、首帧延迟显示成 14089 ms，
    看上去像"补位进来的人被亏待了"（实际是 29.6 fps / 21 ms）。用户等待总时长另算一列。
40. **`tasklist` / `taskkill` 在 Git Bash 里必须加 `MSYS_NO_PATHCONV=1`**：
    否则 `/FI`、`/F` 被当成路径转换掉，报"无效参数"，而**命令静默失败**——
    表现是"强杀没执行"，一整组实验白跑。
41. **压测工具的 `-drop 秒:序号` 是"接通后第 N 秒"，不是"程序启动后第 N 秒"**。
    给一个还没抢到名额的观众配 `-drop`，它压根没接通，drop 静默不触发，
    测了个寂寞（本次踩过，浪费一组实验）。
42. **`IsWindowVisible` 不能用来判断"有没有最小化"**：最小化的窗口**仍然带 `WS_VISIBLE` 样式位**，
    探针实测"可见=true 最小化=true"，两者同时成立。判据必须是 `IsIconic`。
    这个坑会让人把"已经最小化成功"误判成失败，进而去改本来正确的代码。
    另注：`ShowWindow` 的返回值是"调用**前**窗口是否可见"，返回 0 也不代表失败。
43. **Win32 调用不要自己把 `&pid` 转成 `uintptr` 传给 `LazyProc.Call`**：
    `proc.Call(hwnd, uintptr(unsafe.Pointer(&pid)))` 里 pid 拿不到（实测恒为 0），
    表现为"每个窗口都被判成别的进程、全部跳过"，最后报一个看起来像"窗口不存在"的错，
    而窗口明明好端端在屏幕上。**有封装就用封装**（`windows.GetWindowThreadProcessId`）；
    返回指针的自定义 proc 要用 `windows.Handle`+正确签名的方式，别裸转。
44. **`-auto share` 模式下 `startShare` 跑在 `shell.Run(ctx)` 之前**，那一刻 Gio 窗口还没创建。
    任何"要操作窗口"的逻辑都必须容错重试，不能假定窗口一定存在
    （交互模式一定存在，所以这个坑只在自动化入口暴露，容易漏）。
