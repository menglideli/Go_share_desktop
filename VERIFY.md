# 阶段 0 技术验证报告

> 实测日期：2026-09-23 · 机器：Win10 企业版 LTSC 17763 / i7-13700H(20 线程) / Intel Iris Xe / 双屏 2880×1800+1920×1080
> 全部在 `CGO_ENABLED=0` 下构建运行。复现代码在 `spike/` 各子目录。

---

## 0. 结论摘要

| 层 | 原计划选型 | 验证结果 | 最终决定 |
|---|---|---|---|
| 采集 | `go-mswin/screencapture` | ✅ **通过**，DXGI Duplication 在真实 GPU 上可用（作者原标注"未证明"） | **保留** |
| 编码 | `go.264` H.264（MF 硬编） | ❌ **失败**：MF 路径永久挂起；CPU 路径 1080p 仅 13.6fps | **更换为 Motion JPEG** |
| 传输 | `pion/webrtc/v4` | ✅ **通过**：建连 533ms，单向延迟 1.71ms，零丢包 | **保留** |
| UI | Gio（失败回退 walk） | ✅ **通过**：`CGO_ENABLED=0` 构建并渲染成功，窗口真实可见 | **锁定 Gio**，不需回退 |

**端到端延迟（实测各段相加）**

| 分辨率 | 采集 | 转换+编码 | 网络 | 解码 | 渲染(估) | **合计** |
|---|---|---|---|---|---|---|
| 2K 2880×1800 | 4.8ms | 9.9ms | 1.7ms | 4.8ms | ~3ms | **≈ 24ms** |
| 1080p | 3.0ms | 4.9ms | 1.7ms | 2.8ms | ~3ms | **≈ 15ms** |

原定目标 <100ms，实测达成 **15~24ms**（腾讯会议通常 200~400ms）。

---

## 1. S0-1 屏幕采集 ✅

复现：`spike/capture`（`capbench.exe`）

**显示器枚举（双屏，全部正确）**

| 显示器 | 物理像素 | 逻辑 DIP | DPI | scale | Duplicable |
|---|---|---|---|---|---|
| `\\.\DISPLAY1` | 2880×1800 | 1646×1029 | 168 | 1.75 | ✅ |
| `\\.\DISPLAY2` | 1920×1080 | 1920×1080 | 96 | 1.00 | ✅ |

**采集结果（BackendAuto → `duplication`）**

| 显示器 | Backend | MeanCapture | 实测 fps | Stride | DPI 正确性 |
|---|---|---|---|---|---|
| 2K | duplication | **4.83ms** | 30.7 | 11520 (=W×4) | ✅ 采集尺寸 == 物理像素 |
| 1080p | duplication | **3.00ms** | 30.8 | 7680 (=W×4) | ✅ |

**关键结论**

1. **DXGI Duplication 在真实 GPU 路径上验证通过** —— 库作者明确标注"GPU 路径从未执行过"，我们在 Iris Xe 上首次跑通，且性能极好。原 R1 风险解除。
2. **DPI 感知由库内部正确处理**，采到的就是物理像素（2880×1800），无需我们自己设 DPI awareness。原 R7 风险解除。
3. ⚠️ **`ShowsCursor=true` 或"非原生尺寸"会强制回退 GDI，采集耗时 4.8ms → 58.3ms（12 倍），帧率掉到 17fps**。
   → **光标必须自己叠加绘制**，不能依赖库的 `ShowsCursor`。这是 P0-3 的实现约束。
4. ⚠️ 本机 `Stride == Width×4`，但代码仍必须按 `Stride` 索引（其他机器可能对齐到 256）。

---

## 2. S0-2 / S0-6 编码 ❌ → 改方案

复现：`spike/mfprobe`、`spike/encscan`、`spike/codecbench`

### 2.1 Media Foundation 硬件编码：永久挂起

| 尝试 | 结果 |
|---|---|
| `NewEncoder()` 硬件优先 | ✅ 成功，`Backend()=="mediafoundation"`，耗时 392ms |
| `Encode()` 首帧 | ⛔ **超过 15s 不返回，判定为挂起** |
| + `runtime.LockOSThread()`（COM 线程亲和性） | ⛔ 同样挂起 |
| + `CoInitializeEx(MULTITHREADED)`（COM 初始化） | ⛔ 同样挂起 |

三种假设全部失败 → **go264 v1.11.0（发布仅 2 天）的 MF 编码路径在本机死锁，不可用**。

### 2.2 CPU 路径：性能不足

| 分辨率 | 配置 | ms/帧 | fps |
|---|---|---|---|
| 2880×1800 | H.264 QP24 CABAC slices=20 | 157.2 | **6.4** |
| 1920×1080 | H.264 QP24 CABAC slices=20 | 73.4~77.6 | **12.9~13.6** |
| 1280×720 | H.264 QP24 CABAC slices=20 | 56.0 | 17.9 |

即使 720p 也只有 17.9fps。**全部不达标**。

> ⚠️ **差点让验证失效的坑**：中途一次测试得到"1080p 28ms/帧（35.7fps）"的乐观数字，
> 原因是那段代码把**同一帧反复喂给编码器**，桌面静止内容被大量 skip。
> 改用逐帧滚动内容后变成 73.4ms。**性能测量必须使用变化的帧**。

> ⚠️ **反直觉陷阱**：所谓"极速配置"（`MotionSearchZero`）反而更慢（1080p 64ms→175ms），
> 因为零运动搜索让运动补偿失效，宏块全部转为 intra 编码，帧大小从 3KB 暴涨到 1MB。

---

## 3. 方案变更：改用 Motion JPEG（核心决策）

复现：`spike/jpegpar`（`jpegpar.exe`）

既然 H.264 走不通，重新审视场景约束：**内网千兆，带宽不是瓶颈，延迟和 CPU 才是。**
Motion JPEG 恰好命中这一点，且用标准库即可。

### 3.1 编码耗时（含 BGRA→I420 转换，20 线程并行，20 条带）

| 分辨率 | q=75 | q=85 |
|---|---|---|
| 2880×1800 | **9.9ms（101fps）** | 10.6ms（94.6fps） |
| 1920×1080 | **4.9ms（206fps）** | 5.0ms（200fps） |

条带数与耗时（1080p q75）：1 条带 39.9ms → 4 条带 12.9ms → 8 条带 9.3ms → **20 条带 4.9ms**。
标准库 `image/jpeg` 是单线程的，**按水平条带并行后提速约 8 倍**，这是可行的关键。

### 3.2 解码耗时（观看端）

| 分辨率 | 1 条带 | 8 条带 | 20 条带 |
|---|---|---|---|
| 2880×1800 | 64.2ms | 11.2ms | **4.8ms（210fps）** |
| 1920×1080 | 25.8ms | 4.2ms | **2.8ms（364fps）** |

### 3.3 MJPEG 带来的连锁简化

| 原 H.264 方案的难题 | MJPEG 下的情况 |
|---|---|
| MF 硬编不支持 `ForceKeyFrame` → 需 GOP 环形缓存才能秒开 | **每帧独立，新观众天然秒开**，GOP 缓存整个不需要 |
| 新观众要等一个 GOP（0.5~1s） | **0ms** |
| 丢 P 帧会花屏、需要 IDR 恢复 | **丢帧只影响该帧**，不花屏、不累积 |
| 分辨率切换要检测 SPS 变化、重建解码器 | **天然支持**，每帧自带尺寸信息 |
| B/P 帧重排序带来额外延迟 | **无重排序**，延迟最低 |
| 依赖 go264（新库、有 bug） | **零外部依赖**，标准库实现 |

### 3.4 代价与对策

**带宽**（30fps 满载，测试画面为最难的高频文本）

| 分辨率 | 帧大小 | Mbps@30 |
|---|---|---|
| 2880×1800 q75 | 1.71MB | 410 |
| 1920×1080 q75 | 680KB | 165 |

千兆内网（~940Mbps）可承载，但 2K 占 41%。对策：

1. **Dirty tile 增量更新（强烈建议，v1 必做）**：屏幕大部分时间只有小区域变化（鼠标、光标、输入光标）。
   按条带比对，只编码+发送变化的条带 → CPU 与带宽可再降数倍。这是 MJPEG 独有的优势，H.264 做不了这么细。
2. 质量档位：清晰 q85 / 均衡 q75 / 流畅 q65 或降分辨率。
3. 静止时降帧率（DXGI 变化驱动，桌面不动就不发包）。

---

## 4. S0-4 传输 ✅

复现：`spike/rtc`（`rtcprobe.exe`），同进程两 PeerConnection（仍经真实 UDP socket + ICE + DTLS）

| 指标 | 实测 |
|---|---|
| ICE + DTLS 建连 | **533ms**（<1s ✅） |
| 单向延迟（64KB × 100） | **1.71ms** |
| 丢包 | **0%**（480/480 片全部到达） |
| 吞吐（30fps 节奏） | **162.9 Mbps** |

DataChannel 采用 `Ordered=false, MaxRetransmits=0`（类 UDP 语义），避免重传阻塞。

⚠️ 注意：2K MJPEG 需要 410Mbps，高于本次实测的 162Mbps（该值受 30fps 节奏限制，非上限）。
**v1 建议**：默认 1080p（165Mbps），2K 作为"清晰模式"可选，并尽快落地 dirty tile 优化。

---

## 5. S0-5 Gio UI ✅

复现：`spike/ui`（`uiprobe.exe`）

- `CGO_ENABLED=0` **构建通过**（无需 gcc/WebView2，与本机环境约束吻合）
- 窗口创建成功，持续渲染 61 帧无异常，2.9s 后正常关闭
- Win32 `FindWindowW` + `IsWindowVisible` 断言：**窗口真实可见**（`visible=1`）
- 验证了三条渲染路径：纯色背景、位图（视频帧渲染前置验证）、文本

> ⚠️ **差点误判的坑**：第一帧时 `IsWindowVisible` 返回 0，一度怀疑窗口没显示出来。
> 实际是 Gio 尚未完成 `ShowWindow`；强制持续渲染到第 10 帧再断言即 `visible=1`。

**UI 锁定 Gio，不需要回退 walk。**

---

## 6. S0-3 转换与缩放 ✅（前期已完成）

复现：`spike/scalebench`

| 操作（2880×1800） | 并行 20 线程 |
|---|---|
| BGRA→I420 | 4.29ms |
| 完整流水线 [A] 原生直发 2K | 4.21ms |

---

## 7. S0-7 / S0-8 解码与分辨率切换 ✅

- go264 解码：`Backend()` 首次为 `cpu`（1080p 38.1ms/帧），后续实例为 `mediafoundation-d3d11`；8/8 帧全部正确解出。
  改用 MJPEG 后此项不再是关键路径（MJPEG 解码 2.8~4.8ms）。
- **分辨率切换：go264 的 `Decoder` 支持原地重配置**（实测 8 帧 1920×1080 → 8 帧 1280×720 全部正确输出）。
  原 R12 风险解除；且 MJPEG 下此项本就天然成立。

---

## 8. 阶段 0 判定

| # | Spike | 判定 |
|---|---|---|
| S0-1 | 采集：枚举/DXGI/双屏/DPI/帧率 | ✅ 通过 |
| S0-2 | go264 编码（MF 硬编 + CPU） | ❌ 不通过 → 更换方案 |
| S0-3 | BGRA→I420 转换与缩放 | ✅ 通过 |
| S0-4 | pion P2P 建连与延迟 | ✅ 通过 |
| S0-5 | Gio `CGO_ENABLED=0` 构建与渲染 | ✅ 通过 |
| S0-6 | 2K 编码耗时 | ❌ H.264 不通过 → **MJPEG 通过（101fps）** |
| S0-7 | 解码耗时 | ✅ 通过（MJPEG 2.8~4.8ms） |
| S0-8 | 流中途分辨率变化 | ✅ 通过（原地重配置 / MJPEG 天然支持） |

**选型最终锁定：screencapture + Motion JPEG（标准库，条带并行）+ pion/webrtc + Gio。**
不再依赖 `go.264`（该库在新机器上有 MF 死锁问题，且 CPU 性能不足）。

---

## 9. 后续待办

- [ ] 向 go264 作者反馈 MF 编码死锁（对社区有价值，也留一条未来回退 H.264 的路）
- [ ] Dirty tile 增量更新方案设计（v1 必做，直接决定带宽与 CPU）
- [ ] 光标叠加实现（因 `ShowsCursor` 会强制 GDI，必须自绘）
- [ ] 进入阶段 1：采集与预览
