# 阶段 0 技术验证报告

> 实测日期：2026-09-23 · 机器：Win10 企业版 LTSC 17763 / i7-13700H(20 线程) / Intel Iris Xe / 双屏 2880×1800+1920×1080
> 全部在 `CGO_ENABLED=0` 下构建运行。复现代码在 `spike/` 各子目录。

---

## 0. 结论摘要

| 层 | 原计划选型 | 验证结果 | 最终决定 |
|---|---|---|---|
| 采集 | `go-mswin/screencapture` | ✅ **通过**，DXGI Duplication 在真实 GPU 上可用（作者原标注"未证明"） | **保留** |
| 编码 | `go.264` H.264（MF 硬编） | ❌ **失败**：MF 路径永久挂起；CPU 路径 1080p 仅 13.6fps | **更换为 Motion JPEG** |
| 传输 | `pion/webrtc/v4` | ✅ **通过**：建连 533ms，单向延迟 1.71ms，零丢包 | **保留** |
| UI | Gio（失败回退 walk） | ✅ **通过**：`CGO_ENABLED=0` 构建并渲染成功，窗口真实可见 | **锁定 Gio**，不需回退 |

**端到端延迟（实测各段相加）**

| 分辨率 | 采集 | 转换+编码 | 网络 | 解码 | 渲染(估) | **合计** |
|---|---|---|---|---|---|---|
| 2K 2880×1800 | 4.8ms | 9.9ms | 1.7ms | 4.8ms | ~3ms | **≈ 24ms** |
| 1080p | 3.0ms | 4.9ms | 1.7ms | 2.8ms | ~3ms | **≈ 15ms** |

原定目标 <100ms，实测达成 **15~24ms**（腾讯会议通常 200~400ms）。

---

## 1. S0-1 屏幕采集 ✅

复现：`spike/capture`（`capbench.exe`）

**显示器枚举（双屏，全部正确）**

| 显示器 | 物理像素 | 逻辑 DIP | DPI | scale | Duplicable |
|---|---|---|---|---|---|
| `\\.\DISPLAY1` | 2880×1800 | 1646×1029 | 168 | 1.75 | ✅ |
| `\\.\DISPLAY2` | 1920×1080 | 1920×1080 | 96 | 1.00 | ✅ |

**采集结果（BackendAuto → `duplication`）**

| 显示器 | Backend | MeanCapture | 实测 fps | Stride | DPI 正确性 |
|---|---|---|---|---|---|
| 2K | duplication | **4.83ms** | 30.7 | 11520 (=W×4) | ✅ 采集尺寸 == 物理像素 |
| 1080p | duplication | **3.00ms** | 30.8 | 7680 (=W×4) | ✅ |

**关键结论**

1. **DXGI Duplication 在真实 GPU 路径上验证通过** —— 库作者明确标注"GPU 路径从未执行过"，我们在 Iris Xe 上首次跑通，且性能极好。原 R1 风险解除。
2. **DPI 感知由库内部正确处理**，采到的就是物理像素（2880×1800），无需我们自己设 DPI awareness。原 R7 风险解除。
3. ⚠️ **`ShowsCursor=true` 或"非原生尺寸"会强制回退 GDI，采集耗时 4.8ms → 58.3ms（12 倍），帧率掉到 17fps**。
   → **光标必须自己叠加绘制**，不能依赖库的 `ShowsCursor`。这是 P0-3 的实现约束。
4. ⚠️ 本机 `Stride == Width×4`，但代码仍必须按 `Stride` 索引（其他机器可能对齐到 256）。

---

## 2. S0-2 / S0-6 编码 ❌ → 改方案

复现：`spike/mfprobe`、`spike/encscan`、`spike/codecbench`

### 2.1 Media Foundation 硬件编码：永久挂起

| 尝试 | 结果 |
|---|---|
| `NewEncoder()` 硬件优先 | ✅ 成功，`Backend()=="mediafoundation"`，耗时 392ms |
| `Encode()` 首帧 | ⛔ **超过 15s 不返回，判定为挂起** |
| + `runtime.LockOSThread()`（COM 线程亲和性） | ⛔ 同样挂起 |
| + `CoInitializeEx(MULTITHREADED)`（COM 初始化） | ⛔ 同样挂起 |

三种假设全部失败 → **go264 v1.11.0（发布仅 2 天）的 MF 编码路径在本机死锁，不可用**。

### 2.2 CPU 路径：性能不足

| 分辨率 | 配置 | ms/帧 | fps |
|---|---|---|---|
| 2880×1800 | H.264 QP24 CABAC slices=20 | 157.2 | **6.4** |
| 1920×1080 | H.264 QP24 CABAC slices=20 | 73.4~77.6 | **12.9~13.6** |
| 1280×720 | H.264 QP24 CABAC slices=20 | 56.0 | 17.9 |

即使 720p 也只有 17.9fps。**全部不达标**。

> ⚠️ **差点让验证失效的坑**：中途一次测试得到"1080p 28ms/帧（35.7fps）"的乐观数字，
> 原因是那段代码把**同一帧反复喂给编码器**，桌面静止内容被大量 skip。
> 改用逐帧滚动内容后变成 73.4ms。**性能测量必须使用变化的帧**。

> ⚠️ **反直觉陷阱**：所谓"极速配置"（`MotionSearchZero`）反而更慢（1080p 64ms→175ms），
> 因为零运动搜索让运动补偿失效，宏块全部转为 intra 编码，帧大小从 3KB 暴涨到 1MB。

---

## 3. 方案变更：改用 Motion JPEG（核心决策）

复现：`spike/jpegpar`（`jpegpar.exe`）

既然 H.264 走不通，重新审视场景约束：**内网千兆，带宽不是瓶颈，延迟和 CPU 才是。**
Motion JPEG 恰好命中这一点，且用标准库即可。

### 3.1 编码耗时（含 BGRA→I420 转换，20 线程并行，20 条带）

| 分辨率 | q=75 | q=85 |
|---|---|---|
| 2880×1800 | **9.9ms（101fps）** | 10.6ms（94.6fps） |
| 1920×1080 | **4.9ms（206fps）** | 5.0ms（200fps） |

条带数与耗时（1080p q75）：1 条带 39.9ms → 4 条带 12.9ms → 8 条带 9.3ms → **20 条带 4.9ms**。
标准库 `image/jpeg` 是单线程的，**按水平条带并行后提速约 8 倍**，这是可行的关键。

### 3.2 解码耗时（观看端）

| 分辨率 | 1 条带 | 8 条带 | 20 条带 |
|---|---|---|---|
| 2880×1800 | 64.2ms | 11.2ms | **4.8ms（210fps）** |
| 1920×1080 | 25.8ms | 4.2ms | **2.8ms（364fps）** |

### 3.3 MJPEG 带来的连锁简化

| 原 H.264 方案的难题 | MJPEG 下的情况 |
|---|---|
| MF 硬编不支持 `ForceKeyFrame` → 需 GOP 环形缓存才能秒开 | **每帧独立，新观众天然秒开**，GOP 缓存整个不需要 |
| 新观众要等一个 GOP（0.5~1s） | **0ms** |
| 丢 P 帧会花屏、需要 IDR 恢复 | **丢帧只影响该帧**，不花屏、不累积 |
| 分辨率切换要检测 SPS 变化、重建解码器 | **天然支持**，每帧自带尺寸信息 |
| B/P 帧重排序带来额外延迟 | **无重排序**，延迟最低 |
| 依赖 go264（新库、有 bug） | **零外部依赖**，标准库实现 |

### 3.4 代价与对策

**带宽**（30fps 满载，测试画面为最难的高频文本）

| 分辨率 | 帧大小 | Mbps@30 |
|---|---|---|
| 2880×1800 q75 | 1.71MB | 410 |
| 1920×1080 q75 | 680KB | 165 |

千兆内网（~940Mbps）可承载，但 2K 占 41%。对策：

1. **Dirty tile 增量更新（强烈建议，v1 必做）**：屏幕大部分时间只有小区域变化（鼠标、光标、输入光标）。
   按条带比对，只编码+发送变化的条带 → CPU 与带宽可再降数倍。这是 MJPEG 独有的优势，H.264 做不了这么细。
2. 质量档位：清晰 q85 / 均衡 q75 / 流畅 q65 或降分辨率。
3. 静止时降帧率（DXGI 变化驱动，桌面不动就不发包）。

---

## 4. S0-4 传输 ✅

复现：`spike/rtc`（`rtcprobe.exe`），同进程两 PeerConnection（仍经真实 UDP socket + ICE + DTLS）

| 指标 | 实测 |
|---|---|
| ICE + DTLS 建连 | **533ms**（<1s ✅） |
| 单向延迟（64KB × 100） | **1.71ms** |
| 丢包 | **0%**（480/480 片全部到达） |
| 吞吐（30fps 节奏） | **162.9 Mbps** |

DataChannel 采用 `Ordered=false, MaxRetransmits=0`（类 UDP 语义），避免重传阻塞。

⚠️ 注意：2K MJPEG 需要 410Mbps，高于本次实测的 162Mbps（该值受 30fps 节奏限制，非上限）。
**v1 建议**：默认 1080p（165Mbps），2K 作为"清晰模式"可选，并尽快落地 dirty tile 优化。

---

## 5. S0-5 Gio UI ✅

复现：`spike/ui`（`uiprobe.exe`）

- `CGO_ENABLED=0` **构建通过**（无需 gcc/WebView2，与本机环境约束吻合）
- 窗口创建成功，持续渲染 61 帧无异常，2.9s 后正常关闭
- Win32 `FindWindowW` + `IsWindowVisible` 断言：**窗口真实可见**（`visible=1`）
- 验证了三条渲染路径：纯色背景、位图（视频帧渲染前置验证）、文本

> ⚠️ **差点误判的坑**：第一帧时 `IsWindowVisible` 返回 0，一度怀疑窗口没显示出来。
> 实际是 Gio 尚未完成 `ShowWindow`；强制持续渲染到第 10 帧再断言即 `visible=1`。

**UI 锁定 Gio，不需要回退 walk。**

---

## 6. S0-3 转换与缩放 ✅（前期已完成）

复现：`spike/scalebench`

| 操作（2880×1800） | 并行 20 线程 |
|---|---|
| BGRA→I420 | 4.29ms |
| 完整流水线 [A] 原生直发 2K | 4.21ms |

---

## 7. S0-7 / S0-8 解码与分辨率切换 ✅

- go264 解码：`Backend()` 首次为 `cpu`（1080p 38.1ms/帧），后续实例为 `mediafoundation-d3d11`；8/8 帧全部正确解出。
  改用 MJPEG 后此项不再是关键路径（MJPEG 解码 2.8~4.8ms）。
- **分辨率切换：go264 的 `Decoder` 支持原地重配置**（实测 8 帧 1920×1080 → 8 帧 1280×720 全部正确输出）。
  原 R12 风险解除；且 MJPEG 下此项本就天然成立。

---

## 8. 阶段 0 判定

| # | Spike | 判定 |
|---|---|---|
| S0-1 | 采集：枚举/DXGI/双屏/DPI/帧率 | ✅ 通过 |
| S0-2 | go264 编码（MF 硬编 + CPU） | ❌ 不通过 → 更换方案 |
| S0-3 | BGRA→I420 转换与缩放 | ✅ 通过 |
| S0-4 | pion P2P 建连与延迟 | ✅ 通过 |
| S0-5 | Gio `CGO_ENABLED=0` 构建与渲染 | ✅ 通过 |
| S0-6 | 2K 编码耗时 | ❌ H.264 不通过 → **MJPEG 通过（101fps）** |
| S0-7 | 解码耗时 | ✅ 通过（MJPEG 2.8~4.8ms） |
| S0-8 | 流中途分辨率变化 | ✅ 通过（原地重配置 / MJPEG 天然支持） |

**选型最终锁定：screencapture + Motion JPEG（标准库，条带并行）+ pion/webrtc + Gio。**
不再依赖 `go.264`（该库在新机器上有 MF 死锁问题，且 CPU 性能不足）。

---

## 9. 后续待办

- [ ] 向 go264 作者反馈 MF 编码死锁（对社区有价值，也留一条未来回退 H.264 的路）
- [ ] Dirty tile 增量更新方案设计（v1 必做，直接决定带宽与 CPU）
- [ ] 光标叠加实现（因 `ShowsCursor` 会强制 GDI，必须自绘）
- [ ] 进入阶段 1：采集与预览

---

# 阶段 2 追加修复记录

> 实测日期：2026-09-24 · 触发条件：sharedemo 观看窗"全黑，看不见东西"

## 10. 观看窗全黑 —— 根因是状态栏背景把画面盖住了

### 10.1 现象与排查路径

现象：`sharedemo.exe` 观看窗内一片黑，仅底部状态栏文字可见；日志显示已渲染 889 帧、`画面 true`。

链条上四个观测点分别实测（`cmd/pixprobe` + `cmd/shot`）：

| 观测点 | 实测结果 | 判定 |
|---|---|---|
| 采集源 | 非黑 98.2%、亮度 212 | ✅ 正常 |
| 编码条带 | 17/17 条带、172KB | ✅ 正常 |
| 解码画布 | 非黑 98.1%、亮度 212.4 | ✅ 正常 |
| **窗口实际像素（截图）** | **图像区域纯黑，状态栏文字清晰** | ❌ **问题在这里** |

关键判据：**Gio 能把状态栏文字画出来，说明渲染管线是活的**，因此问题不在 GPU/纹理路径，
而在"画了但被盖住"。

### 10.2 根因

`internal/ui/viewer.go` 的 `layoutStatusBar2`：

```go
paint.ColorOp{Color: colPanel}.Add(gtx.Ops)
paint.PaintOp{}.Add(gtx.Ops)     // ← 没有 clip
```

Gio 的 `paint.PaintOp` 填充的是**当前裁剪区域**。`layout.Rigid` 不会替调用方设置裁剪，
因此这一句把面板色（22,28,34）铺满了整个窗口——包括上方刚画好的视频区域。
文字是之后画的，所以只有文字幸存。

对照：阶段 1 的 `preview.go` 里**有** `clip.Rect`，阶段 2 重写观看端时漏掉了。

```go
// 阶段 1 的写法（有限制，但仍不严谨）
rect := clip.Rect{Max: gtx.Constraints.Max}.Push(gtx.Ops)
```

而 `layout.Rigid` 内的 `gtx.Constraints.Max` 是**整个窗口尺寸**（Flex 不会给 Rigid 设高度上界），
所以这写法照样会盖住画面——两处最终都统一改成"**先量内容拿到真实高度，再按该高度裁剪**"。

### 10.3 反向变异验证

| 版本 | 结果 |
|---|---|
| 有 clip（修复版） | 画面正常显示，状态栏 28.7fps / 7Mbps ✅ |
| 去掉 clip（变异版） | **立刻复现全黑**，只剩状态栏文字 ❌ |

唯一差别就是那一行裁剪 → 因果链闭合。

### 10.4 顺带实测到的另一个真实缺陷（未修，已登记）

**DXGI Duplication 建立后的首帧可能是未初始化的黑帧**：

| 运行次数 | DISPLAY1 首帧 | DISPLAY2 首帧 |
|---|---|---|
| 第 1 次 | 0.0% 非黑（黑） | 0.0% 非黑（黑） |
| 第 2 次 | 99.4% 非黑（正常） | 0.0% 非黑（黑） |
| 第 3 次 | — | 0.0% 非黑（黑） |

第二帧起全部正常。影响：观众端开屏闪一帧黑；桌面静止时这帧黑会一直挂到下次变化。
**未修原因**：修复需动采集核心路径（预热丢帧 + 有限超时），且不能无条件丢首帧
（静止桌面会永远等不到画面）。登记为 R25，阶段 3 接入多观众前处理。

### 10.5 最重要的教训：验证方法本身有盲区

阶段 1/2 对"画面能显示"的验证只用了 `IsWindowVisible` + 渲染帧计数 ——
**这两项在画面被完全盖住时依然全部通过**。也就是说这个 bug 能穿过整个阶段 2 的验证。

结论（已写入 PLAN R26）：**凡涉及画面输出的改动，必须用像素证据收口**。
新增三个可复用工具：

| 工具 | 用途 |
|---|---|
| `cmd/shot` | 截取指定显示器存 PNG —— 看窗口里到底画出了什么 |
| `cmd/pixprobe` | 逐环节统计非黑占比/平均亮度 —— 定位黑在哪一环 |
| `cmd/viewertest` | 隔离渲染层与数据层 —— 二分"是数据黑还是没画出来" |

---

## 11. 阶段 3：信令与接入（2026-09-24）

### 11.1 多网卡枚举（`internal/netif`）

本机实测枚举出 **6 个可分享地址**，默认路由出口排首位：

| IP | 网卡 | 类型 | 默认路由 |
|---|---|---|---|
| 192.168.250.180 | 以太网 | 物理 | ✅ |
| 192.168.11.248 | ZeroTier | 虚拟 | |
| 192.168.9.138 | ZeroTier | 虚拟 | |
| 192.168.246.1 | VMware | 虚拟 | |
| 192.168.28.1 | Hyper-V | 虚拟 | |
| 172.17.51.33 | Docker | 虚拟 | |

过滤规则：排除回环、`169.254.*`（APIPA）、非 Up 接口。虚拟网卡标注类型但**不排除**
（ZeroTier 内网穿透恰好要靠它）。4 个 `169.254.*` 陈旧地址被正确过滤。

### 11.2 信令自检（`cmd/sigcheck`）

**29/29 通过**。覆盖：授权码正确/错误/限次冷却、观众上限、SDP 交换、
non-trickle ICE、DataChannel 双向就绪、bye 回收。

**踩坑**：初次跑 21/22，失败项是"answer 里没有 ice-ufrag"。写最小复现打印两端 SDP 才看清——
**观看端在 `CreateOffer` 之前没有创建任何 data channel，pion 就不会生成 `m=application` 段**，
SDP 里连 ice-ufrag 都没有。给观看端补 `SetupViewer()`（主动建一条通道）后修复。

### 11.3 端到端接入实测（`cmd/sigdemo`）

全部走**局域网真实 IP**（`192.168.250.180:9000`，非回环），跨进程：

| 场景 | 结果 |
|---|---|
| 指定地址接入 | 成功，握手 531 / 889 ms，27.4 fps / 8.2 Mbps |
| 自动发现接入（`-scan`） | 成功，UDP 广播扫到会话并接入，窗口标题带分享方主机名 |
| 两观众并发 | 分享端「当前 2 人」，各自独立 Peer 互不影响 |
| 观众正常退出 | 「已释放（正常断开），剩余 0 人」 |
| 观众被强杀（无 Bye） | ICE `disconnected` 超时 5s 后回收，剩余 0 人 |

### 11.4 R25 修复：DXGI 首帧黑

`capture.Source.Warmup()` 预热：丢弃未初始化的黑帧，**有限次数（3）+ 超时（600ms）兜底**，
且**预热期不更新 `Last()`** —— 否则静止桌面会把黑帧当心跳发出去，观众永远等不到画面。

| 组 | 首帧黑次数 |
|---|---|
| 不预热（对照） | **10 / 10** |
| 预热后 | **0 / 10** |

已固化进 `capcheck` 第 12 项，防回归。

### 11.5 观众退出导致分享端静默卡死（R27 / R28）

**症状**：第一个观众退出后，分享端日志停在 `连接状态 closed` 再无输出；
**后续任何观众接入都 `context deadline exceeded`**，进程既不崩也不报错 —— 纯静默卡死。
第二次接入时还看到"当前 2 人"（幽灵观众）。

**根因（两层，都在同一个回调链上）**：

1. `OnState(closed)` 回调里**同步**调用 `peer.Close()`。回调跑在 ICE agent 的内部
   goroutine 上，而 Close 要等 agent 收尾 → 互等死锁（R27）。
2. 清理函数用 `sync.Once` 做幂等，而 cleanup → `Server.Drop` → `closeFn` → cleanup
   是**同 goroutine 重入**，`once.Do` 永久阻塞（R27）。
3. 顺带查出的第三处：`Server.Kick` 持 `s.mu` 调 `closeFn`，而 closeFn 回头调
   `srv.Detach` → 同一把锁自锁（R28）。

**修复**：清理动作 `go cleanup(...)` 挪出回调线程；幂等改 `mu + bool flag`；
`Server` 拆出 `Detach`（只摘记录）供 cleanup 用，`Kick` 改为"先摘完、解锁、再回调"。

**验证**（修复前后对照，同一测试步骤）：

| 步骤 | 修复前 | 修复后 |
|---|---|---|
| 观众 1 接入 → 退出 | 日志停在 `closed`，无回收记录 | `已释放（正常断开），剩余 0 人` |
| 观众 2 接入 | **超时失败**（分享端已卡死） | 成功接入，「当前 1 人」 |
| 观众 2 退出 | — | `已释放（正常断开），剩余 0 人` |
| 两观众并发 | — | 「当前 2 人」，双双正常回收 |

### 11.6 本次踩到的验证方法坑

**用 `cmd &` 起后台进程会被工具调用回收**：验证双观众并发时用 `./sigdemo.exe ... &`
起观看端，Bash 调用一返回子进程就被杀掉，分享端于是走了"断线超过 5s"兜底路径。
虽然顺手验证了强杀兜底是对的，但**它让"并发在线"这个验证目标失效了** ——
如果只看 host 日志里出现的「剩余 1 人 → 剩余 0 人」，很容易把结论误读成"并发测试通过"。
起长生命周期验证进程必须用 `run_in_background`（已写入 PLAN 阶段 3 约束 18）。

**截图验证在锁屏时不可用**：`cmd/shot` 截到的是 Windows 锁屏画面（纯蓝），
此时像素证据无效。画面类验证必须在会话 Active 时做 —— 与 R26 的教训叠加：
"有像素证据"和"像素证据有效"是两件事。

---

## 12. 阶段 4：界面与质量（首页双入口 / 质量档位 / 诊断 HUD / 暂停 / 框选）

### 12.1 交付与验证通道

| 交付物 | 文件 |
|---|---|
| 应用外壳（单窗口 + 路由状态机） | `internal/ui/shell.go` |
| 六个页面（首页/设置/分享中/加入/观看中/框选） | `internal/ui/pages.go` |
| 分享端主程序重写（接线 UI 与业务） | `cmd/goshare/main.go` |
| 暂停 + 全黑自诊断 | `internal/pipeline/sharer.go` |
| 离屏渲染校验器 + 基线比对 | `cmd/uicheck/main.go` |
| 渲染基线（入库，回归门禁） | `testdata/ui-golden/ui-*.png` |
| 像素比对小工具（临时取证用） | `spike/pngdiff/main.go` |

**本次会话全程锁屏**，所以验证通道分成两条：

| 通道 | 能验证 | 不能验证 |
|---|---|---|
| `cmd/uicheck`（`gpu/headless` 离屏渲染） | 页面布局、控件是否存在、遮罩/边框、暂停态覆盖层 | 真实窗口行为、鼠标交互 |
| 端到端进程日志 | 连接、握手、帧率、带宽、暂停节奏、告警 | **画面内容**（锁屏下采集本来就是黑的） |

### 12.2 离屏渲染校验：8 / 8 通过

`cmd/uicheck` 用 `headless.NewWindow` 起离屏窗口，按 `layout.Context` 手动喂事件源
（`input.Router`）渲染 8 个页面，判据 `非黑 > 2% 且 不同色 > 8 种`：

```
$ uicheck.exe -golden out/golden
[PASS] home                   非黑 100.0% · 不同色 127 种 · 亮部  2.9% · 与基线差 0.000%
[PASS] setup                  非黑 100.0% · 不同色 145 种 · 亮部  1.2% · 与基线差 0.000%
[PASS] sharing                非黑 100.0% · 不同色 1055 种 · 亮部 32.5% · 与基线差 0.000%
[PASS] sharing-paused         非黑  74.0% · 不同色 614 种 · 亮部 13.2% · 与基线差 0.000%
[PASS] join                   非黑 100.0% · 不同色 123 种 · 亮部  1.1% · 与基线差 0.000%
[PASS] join-error             非黑 100.0% · 不同色 165 种 · 亮部  1.1% · 与基线差 0.000%
[PASS] viewing                非黑  91.5% · 不同色 958 种 · 亮部 69.4% · 与基线差 0.000%
[PASS] region                 非黑 100.0% · 不同色 708 种 · 亮部 53.5% · 与基线差 0.000%

合计 8 通过 / 0 失败
```

几个数字本身就是证据，不只是"通过了"：

- `sharing` 不同色 **1054 种**（其余页面 ~120–160 种）—— 左侧预览区真的把一帧彩色画面画进去了，
  不是一块纯色矩形。
- `sharing-paused` 非黑从 100% 掉到 **74.0%**，不同色从 1054 掉到 **614** —— 暂停遮罩
  实打实盖住了预览区。
- `region` 亮部 **53.5%**、不同色 708 —— 桌面快照背景 + 挖洞遮罩 + 蓝框都在。
- `viewing` 亮部 **69.4%** —— 观看页画面区占大头，符合"画面为主、顶栏为辅"的布局意图。

### 12.3 R31：8/8 通过是假的 —— 粗统计漏掉了 R30

这一步是本次最该记住的坑：**先有"8 个页面全部 PASS"这个结论，后才发现它是不可信的。**

给 Uicheck 补做反向变异时（把 `SetShareState` 里补按钮切片的三行改掉），预期是
"页面会明显变化 → 判据 FAIL"。实测结果：

| 判据 | 变异后 |
|---|---|
| 粗统计（非黑 / 不同色 / 亮部） | **仍然 8/8 PASS** —— sharing 色数 1055→1057、亮部 32.5%→33.8%，"几乎没动" |
| 逐像素比对（`spike/pngdiff`） | **3 个页面实打实变了**：sharing 3.93%、sharing-paused 4.24%、join 2.31% |

也就是说：**"按钮整列消失"这种缺陷，粗粒度像素统计根本测不出来。**
R30 当初能被发现靠的是人眼读图，不是判据 —— 判据一直是绿的。

**修复**：给 `uicheck` 加基线逐像素比对（`-golden <dir>`，差异 >0.5% 判失败、退出码 1）。
前提是渲染确定性已验证：同二进制连跑两次，8 个页面**差异像素均为 0**。

变异版再跑一次，门禁生效：

```
[FAIL] sharing          非黑 100.0% · 不同色 1057 种 · 亮部 33.8% · 与基线差 3.93%（阈值 0.5%）· 差异区 (836,169) 328x588
[FAIL] sharing-paused   非黑  74.0% · 不同色  602 种 · 亮部 13.1% · 与基线差 4.24%（阈值 0.5%）· 差异区 (836,169) 328x587
[FAIL] join             非黑 100.0% · 不同色  130 种 · 亮部  0.8% · 与基线差 2.31%（阈值 0.5%）· 差异区 (304,190) 415x387

合计 5 通过 / 3 失败   （退出码 1）
```

三个差异包围盒都精确落在受影响的区域（右栏地址/观众列表、搜索结果列表），
**其余 5 页 0.000%** —— 判定既抓得住真缺陷，也不误报。

### 12.4 R30：动态列表的按钮没渲染出来

**症状**：分享中页面的地址行右侧「复制」按钮、观众行右侧「踢出」按钮、加入页搜索结果行的
「接入」按钮 —— 在分享状态变化后**整列消失**。

**根因**：`addrCopies` / `kickBtns` / `foundBtns` 是按初始长度建的切片，
`SetShareState` / `SetJoinState` 只更新了状态数据，**没有把按钮切片增长到 `len(状态)`**。
状态变长了，按钮池没变长，多出来的行就没有按钮可画。

**修复**：两个 setter 里按 `len(state)` 补齐按钮切片。

**为什么之前没发现**：日志里地址数量、观众数量全对，只有"画出来没有"能暴露 ——
而真窗口在锁屏下截不到图。是 `cmd/uicheck` 把它照出来的。

**反向变异验证**：把 setter 里的补齐逻辑改掉重跑 → 见 12.3，
**粗判据 8/8 通过、逐像素比对 3 页失败**，症状精确重现。这也说明 R30 之前"通过了"
是判据太粗造成的假绿，不是真的没坏。

### 12.5 端到端：分享 ↔ 观看（同一台机器，走局域网 IP）

分享端 `-auto -port 9000 -code 482913`，观看端 `-addr 192.168.250.180:9000 -code-watch 482913`：

| 项 | 结果 |
|---|---|
| 分享启动 | 端口 9000，授权码 482913，列出 6 个网卡地址 |
| 观看接入 | `已接入 GKYN20230046（192.168.250.180:9000）· 握手 757 ms` |
| 分享侧确认 | `观众 GKYN20230046（192.168.250.180）已接入，当前 1 人` |
| 观众退出 | `已释放（正常断开），剩余 0 人` |

**重试路径单独验证**：先起观看端、后起分享端（分享端晚了 4s）。
修复前这种情况一次拨号就 `deadline exceeded` 且**不打日志**，看上去像"功能坏了"；
修复后 3 次重试 + 4s 间隔命中，接入成功，且每次失败都有日志。

### 12.6 暂停分享：29.3 fps → 1.0 fps

分享端 `-autopause 22s`，观看端统计每 3 秒打一行：

```
12:16:21  已收  87 帧
12:16:24  已收 176 帧   (+89 / 3s ≈ 29.7 fps)
12:16:30  已收 353 帧   (+89 / 3s ≈ 29.7 fps)
12:16:36  已收 529 帧   (+88 / 3s ≈ 29.3 fps)
12:16:37  分享端：自动暂停（-autopause 22s） · 暂停状态 → true
12:16:39  已收 570 帧   (+41 过渡)
12:16:42  已收 573 帧   (+3 / 3s = 1.0 fps)
12:16:45  已收 576 帧   (+3 / 3s = 1.0 fps)
...
12:17:21  已收 611 帧   (+3 / 3s = 1.0 fps)
```

节奏正好落在 `pauseInterval = 1s` 上：**暂停不是"不发"，而是"每秒一帧黑帧"** ——
观众看到的是稳定 1 fps 的黑屏而不是断流，可以据此判断"对方暂停了"。
恢复侧强制全量帧（避免 dirty tile 残影）已写进 `SetPaused(false)`，
本次因锁屏无内容可看，留到解锁复跑时一起验。

### 12.7 R29：锁屏采集全黑 —— 加自诊断，然后日志被刷爆

**第一层（产品问题）**：分享端在锁屏下采集到的是真黑帧，观众只看到黑屏，
而分享端自己毫不知情。加 `BlackScreen()`：每 15 帧抽样一次，步长 8 采样，
非黑占比 < 0.2% 记一次 streak，连续 4 次判定"持续全黑"，HUD 显示告警 + 打日志。

**第二层（修完自己踩的坑）**：HUD 每 500ms 刷一次，告警就每 500ms 打一行 ——

| 版本 | 单次运行（约 30s）告警行数 |
|---|---|
| 未做翻转控制 | **30 行**（每秒 2 行，日志被刷爆） |
| 加 `blackWarned` 翻转控制 | **1 行** |

`e2e-host2.log` = 修复前（30 行），`e2e-host3.log` = 修复后（1 行），两份日志都在仓库里。

**注意别误判**：锁屏下这条告警**必然出现**，是预期行为不是 bug（见 PLAN 约束 26）。

### 12.8 本次新增的验证方法坑

**"8/8 通过"和"界面没问题"不是一回事**（R31，本节最重要的一条）：
粗粒度像素统计只能证明"画布上有东西"，证明不了"该有的东西都还在"。
判据绿了不代表没坏，要有能证伪的对照（基线比对 / 反向变异）才算数。

**离屏渲染验证不了真实交互**：`uicheck` 里框选页是用 `SetRegionRect` 直接塞矩形渲染的，
**拖拽、点击热区本身一次都没被自动验证过** —— 需要解锁后手动拖一次。

**锁屏让"画面内容"整块无法验证**：连接、握手、帧率、暂停节奏、UI 布局都验到了，
唯独"观众看到的画面对不对"验不了，因为源本来就是黑的。已登记为待复跑项。

**离屏渲染会偶发 `0x887a0005`（GPU device removed）panic**：跑 `-save-golden` 后紧接着
再跑一次，出现过首页全黑 + `ReadPixels` 失败 + GPU panic 栈。同一二进制重跑即恢复 8/8。
**看到这个错先重跑再判断**，别把它读成"界面改坏了"（已写入 PLAN 约束 29）。

### 12.9 阶段 4 未覆盖 / 下次解锁桌面后待复跑

1. **画面内容级端到端**：解锁后跑一次分享+观看，用 `cmd/shot` + `cmd/pixprobe`
   确认观众侧非黑占比与亮度和分享端一致（本次源全黑，此项无效）。
2. **交互式框选的真实拖拽**：鼠标按下/拖动/松开全流程，确认窗口坐标 → 桌面像素换算
   （`containRect` + DPI scale）在 175% 主屏上正确。
3. **多显示器切换**（`pageSetup` 的显示器单选）在真实双屏下逐项切一次。
4. **踢人按钮**的真实点击（离屏只验了"按钮画出来了"）。

---

## 13. 阶段 5：健壮性（进行中）—— R32 观众接入缺全量帧

### 13.1 先补做阶段 4 的遗留项：画面内容级复验

桌面已解锁，12.9 里那四项欠账中的第 1 项当场还掉：

| 项 | 数值 |
|---|---|
| 采集源（副屏，同刻截图） | 1920×1080 · 非黑 **98.2%** · 亮度 **208.1** |
| 观看端收到并解码后 | 1920×1080 · 非黑 **98.2%** · 亮度 **209.0** |
| 首帧 | `全量=true 条带 17/17 非黑 98.0%` |

源与观众侧对得上，链路（采集→编码→传输→解码）内容正确。

### 13.2 症状：观众开局只有 11.8% 的画面

顺带做另一次端到端时发现观看端收到第 60 帧的**分带亮度有 5/16 带是 0.0**：

```
watch-frame.png  1920x1080  非黑 50.7%  亮度 115.6
    带  1/16  y    0..  67  亮度    0.0 X
    带  6/16  y  337.. 405  亮度    0.0 X
    带  9/16  y  540.. 607  亮度    0.0 X
    带 10/16  y  607.. 675  亮度    0.0 X
    带 11/16  y  675.. 742  亮度    0.0 X
    → 5 / 16 带是黑的
```

不是"画面偏暗"，是**有条带从来没被填过**。为此写了 `spike/pixstat`（整体 + 分带统计）：
整幅平均会把"一半画了一半黑的画面"平均成一个中间值，看起来像偏暗，分带才能定位黑带位置。

### 13.3 根因：全量帧只发一次，撞在通道未就绪上就永久丢失

三层叠加，缺一层都不会这么严重：

1. **时机**：`sh.AddPeer(peer)` 在 `WaitGathering` 之后立刻调用，而它触发的 `ForceKeyFrame()`
   只是置标志 —— 真正的全量帧要到**下一帧编码**（~33ms 后）才产生。那一刻 DataChannel
   往往还在 DTLS 握手，`SendFrame` 撞上 `ReadyState() != Open` 直接返回 `ErrNotOpen`，
   **这一帧被丢掉**。
2. **不可重复**：`Encode` 末尾无条件 `e.havePrev = true`，于是全量帧**只此一次**，
   除非再有人显式触发。
3. **没人补**：`CtlPLI` 常量早就定义了，但正式链路里**发送端从未发送、分享端从未处理**
   （只有 `netcheck` 自检里用过）。观众端丢了内容也**不知道自己丢了**。

合起来：增量帧只覆盖"变化过的"条带，静止区域就永远是黑的。
实测（变异验证）：观众开局只拿到 **11.8%** 的画面，靠桌面恰好大幅变化触发
"变化面积 > 75% 转全量"才偶然补全 —— **等了 226 帧 / 12 秒**。桌面不动就可能一直黑下去。
这正是用户报的"里面全黑 看不见东西"。

### 13.4 修复：三条**互相独立**的恢复路径

| # | 路径 | 位置 |
|---|---|---|
| ① | 通道就绪后补一次请求：`go peer.WaitReady(15s)` → `sh.RequestKeyFrame()` | `cmd/goshare` accept |
| ② | 接入窗口内密集补帧：新观众接入后 `KeyBurst`(3s) 内每 `KeyBurstGap`(400ms) 补一帧全量 | `pipeline.Sharer` |
| ③ | 观众端主动请求：画面不完整（或检测到分片丢失）→ 发 `CtlPLI` → 分享端 `RequestKeyFrame` | 两侧 |

③ 走可靠有序的 `ctl` 通道，是唯一"接收侧自救"的手段，也是最不会随发送侧逻辑变化而失效的一条。

**刻意不做**：常态下周期性全量帧（`KeyInterval` 默认 0/关闭）。实测开 3s 稳态周期会让
带宽从 **5.5 → 9.2 Mbps（+67%）**，常态恢复交给 ③ 更划算；补帧只发生在接入窗口内。

### 13.5 反向变异验证矩阵（本节的核心）

每一组都是"去掉一部分恢复路径，看画面还能不能自愈"。所有组共用同一个变异前提：
**注释掉 `AddPeer` 里的 `ForceKeyFrame()`**，即模拟"接入那一次全量帧必然失效"。

| 组 | 生效的恢复路径 | 首帧 | 补全耗时 | 结论 |
|---|---|---|---|---|
| **修复前**（e2e10） | 无（三条都没有） | `全量=false 2/17` 非黑 **11.8%** | **226 帧 / 12 秒**（靠偶然） | 复现"开局大片黑" |
| **①②**（e2e17） | 通道就绪请求 + 补帧窗口 | `全量=true 17/17` 非黑 97.8% | 首帧即完整 | 修复有效（两条都没关，不区分） |
| **仅①**（e2e18） | 只留通道就绪请求（`-key-burst=-1s` 关②） | `全量=true 17/17` 非黑 98.2% | 首帧即完整 | **① 独立有效** |
| **仅③**（e2e19） | 只留 PLI | `全量=false 2/17` 非黑 **11.8%** → 下一帧 `17/17` **98.0%** | **1 帧 / ~33ms** | **③ 独立有效，且最快** |

仅③那组的分享端日志留下了确凿的因果链：

```
14:30:54 收到帧 #1 seq=314 全量=false 条带 2/17 非黑 11.8%
14:30:54 观众 127.0.0.1 请求全量帧          ← 观众发现不完整，主动要
14:30:54 收到帧 #2 seq=315 全量=true  条带 17/17 非黑 98.0%
```

**未单独隔离验证的**：②（补帧窗口）。因为 ③ 在观众端始终生效，无法在不改客户端的前提下
把它单独关掉。②的价值在于"不依赖任何一侧的逻辑判断正确"，属于冗余保险 ——
**不宣称它被单独证明过**。

### 13.6 稳态回归（默认参数）

| 项 | 结果 |
|---|---|
| 首帧 | `全量=true 条带 17/17 非黑 98.2%` |
| 全量帧计数 | 接入后 6 → 11，**之后停止增长**（11/478 → 11/748），补帧窗口如期结束 |
| 带宽 | 稳态 0.0~2.3 Mbps（桌面静止），无固定额外开销 |
| 编码均值 | 4.9~7.1 ms |
| 观众回收 | 正常断开 → 剩余 0 人 |

### 13.7 顺带修掉的一处"我修错了"

`HUD` 里的"编码 X ms"我一开始判定为 bug（以为 `Stats.EncodeMs` 是累计值，显示会随时长线性增长），
于是改成 `st.EncodeMs / st.Frames` —— 结果稳定显示 **0.0 ms**。回查才发现
`Sharer.Stats()` 里**已经**做了 `st.EncodeMs /= float64(st.Frames)`，拿到的就是均值，我除重了。
**原来的代码是对的，我改错了**，已改回并把这个坑写进 PLAN 约束 31。

### 13.8 本次新增的验证方法坑（详见 PLAN 约束 32~35）

1. **端口顺延静默污染了整组数据**：host 日志写"端口 9000 被占用，已顺延到 9001"，
   而观看端仍按 9000 去连 —— 连上了**上一个残留的分享端**，"握手成功"看不出异常，整组作废。
   那一刻 9000 上其实没有监听者，是预检误判。**规则：连之前回读 host 日志里的实际端口。**
2. **`rm` 被安全删除机制拦截并 FAIL_CLOSED**：`rm -f e2e*.log` 触发 trash 失败后整条
   `&&` 命令被阻断，症状是"分享端没起来"，极易误判成代码问题。清理日志换文件名即可。
3. **后台 `build && 运行` 的竞态**：两者放同一条后台命令里，运行步骤可能用上**旧二进制**，
   症状是"新加的日志一行都不打"。规则：前台 build 完再起进程。
4. **跨时间的像素基准不可比**：副屏平均亮度 13:59 是 37.5、14:23 是 208。"源 vs 观众"的
   对照必须同刻取样，否则会得出"采错显示器了"这种假结论（本次差点据此改错代码）。

---

## 14. 阶段 5（一）：多人同时观看压测

### 14.1 为什么要单独造一个工具

用 `goshare.exe -auto watch` 开 N 个观看端也能压测，但会开 N 个 Gio 窗口；
窗口本身进了采集画面（自摄入），既污染像素统计也白占 GPU。
所以新造 `spike/multiview`：**无窗口**，一个进程里 N 个观众，各自独立解码器与统计。

```
go run ./spike/multiview -addr 127.0.0.1:10400 -code MV005 -n 5 -dur 40s -stagger 1200ms -drop 20:3
go run ./spike/multiview ... -hard          # 断开时关 Peer 但不发 Bye
go run ./spike/multiview ... -nopli         # 关掉观众侧 PLI 自救（分离恢复路径用）
go run ./spike/multiview ... -retry 9       # 接入失败重试（模拟真实观众端 3 次 × 4s）
```

每个观众单独输出：握手耗时、等待耗时、首帧延迟、**首帧是否全量 / 非黑 / 黑带数**、
帧数、全量帧数、fps、Mbps、解码耗时、**PLI 发出次数与"第几帧发 → 第几帧拿到全量"**、
**首次 16 带全不黑发生在第几帧**。

### 14.2 ⚠️ 差点让整批数据失效的坑：控制台窗口自摄入（PLAN 约束 37）

第一轮（e2e21）分享端采**主屏**，观看端从 bash 启动（Go 控制台程序，会自己弹窗）。
观看端每打印一行日志就滚一次屏 → 采集到的画面持续剧变 → 触发全量帧 → 又打印一行，
**自激振荡**。读数：

| | 被污染（e2e21，主屏） | 干净基线（e2e23，副屏） |
|---|---|---|
| 观众 0 人时的带宽 | **84 → 108 Mbps** | **0.0 ~ 4.2 Mbps** |
| 观众 0 人时的全量帧 | 9 → **186**（3 秒内暴涨） | **1 / 871**（只有首帧） |
| 观众侧每人带宽 | 20 ~ 24 Mbps | 2.6 ~ 2.9 Mbps |

差 10 倍以上。危险在于**它非常有说服力**：fps 正常、帧数正常、画面也"看得到东西"，
只有带宽和全量帧占比不对劲，很容易被归结成"编码器质量档位太高"或者"全量帧策略太激进"，
然后去改编码器——**而那两处本来是对的**。

修法：分享端 `-display 1` 采副屏，控制台窗口留在主屏，根本进不了采集画面。
**判据：只要"观众 0 人"时的带宽不是接近 0，先怀疑自摄入，别急着改编码器。**

### 14.3 3 人同时观看（e2e23，副屏，副屏静止）

| # | 握手 ms | 首帧 ms | 首帧全量 | 首帧非黑 | 首帧黑带 | 帧数 | 全量帧 | fps | Mbps |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 850 | 861 | 否 | 5.9% | 14 | 891 | 16 | 26.3 | 2.85 |
| 2 | 766 | 803 | **是** | 98.1% | 0 | 891 | 12 | 27.5 | 2.75 |
| 3 | 928 | 983 | **是** | 98.1% | 0 | 890 | 7 | 28.8 | 2.63 |

- **完整性恢复**：#1 第 1 帧发 PLI → 第 2 帧拿到全量（**间隔 1 帧 / ~33 ms**，共发 1 次）；#2 / #3 首帧即完整
- **公平性**：帧数 890..891，**极差 0.1%**；fps 26.3..28.8
- 分享端侧：观众 0 人 → 3 人，编码均值 4.2 → 4.5 ms；全量帧接入期 1→18 后**停止增长**

#1 首帧是增量帧（这正是 R32 的原始症状形态），但**观众端 PLI 自救在 1 帧内补上了** ——
说明 R32 的三条恢复路径在多人场景下依然成立。

### 14.4 5 人同时观看 + 中途踢人（e2e24）

| # | 握手 ms | 首帧 ms | 首帧全量 | 首帧非黑 | 帧数 | 全量帧 | fps | Mbps |
|---|---|---|---|---|---|---|---|---|
| 1 | 741 | 785 | **是** | 98.1% | 1186 | 23 | 26.0 | 3.27 |
| 2 | 817 | 841 | **是** | 98.1% | 1186 | 19 | 26.7 | 3.22 |
| 3 | 978 | 986 | **是** | 98.1% | 593(被踢) | 15 | 28.3 | 4.09 |
| 4 | 676 | 702 | **是** | 98.1% | 1186 | 12 | 28.2 | 3.15 |
| 5 | 900 | 939 | **是** | 98.1% | 1188 | 8 | 29.0 | 3.17 |

- **5/5 首帧即完整**（非黑 98.1%，黑带 0）
- **踢人影响面**：#3 在第 20 秒断开，其余 4 人在接下来 12 秒内**各多收 357 帧，分毫不差**，
  fps 从 28.6 升到 29.0 —— **没有影响面**，背压只作用于自己
- 分享端侧：**编码均值 0 人 4.5 ms → 5 人 5.3 ms**，几乎不随人数增长（编码只做一次，N 人只是多发 N 份）；
  fps 稳在 28~30；全量帧到 25 后**停止增长**

### 14.5 人数上限与名额回收（幽灵观众回归）

server.go 里注释过"幽灵观众会一直占着人数名额"这个坑，这里逐条验证名额**确实会释放**：

| 场景 | 结果 |
|---|---|
| `-max 2` + 4 人同时抢（e2e25） | 2 进 2 拒，拒绝干净（`signal: 观看人数已满`），不是超时也不是崩 |
| 有人正常 Bye 退出后补位（e2e26） | 退出后下一次重试**立刻**接入成功（722 ms），首帧即完整 |
| 关 Peer（发 close_notify）后补位（e2e29） | `closed` → **同秒**释放 → 下 1 秒补位成功 |
| **进程被 taskkill 强杀后补位（e2e31）** | `disconnected`（强杀后约 12s）→ **5s 后** `已释放（断线超过 5s）` → 3s 后补位成功 |

e2e31 时间线（host 日志）：
```
15:01:57  观众 127.0.0.1 连接状态 disconnected
15:02:02  观众 127.0.0.1 已释放（断线超过 5s），剩余 1 人
15:02:05  观众 压测-1（127.0.0.1）已接入，当前 2 人     ← 补位成功
```
三种断开方式（正常 Bye / 关 Peer / 进程被强杀）的名额回收**全部验证通过**，
没有出现"幽灵观众永久占名额"。

### 14.6 顺带发现的两个可优化项（不是 bug，已写入 PLAN 待办）

1. **HUD 的 Mbps 是单份编码码率，不是实际出网带宽**。5 人时显示 4.0 Mbps，
   实际出网是 5 份 ≈ 16 Mbps。要么乘人数，要么改标"每路"。
2. **KeyBurst 窗口是全局的**：新观众接入时，已接入的观众也会陪着吃全量帧
   （5 人那组 #1 收到 23 个全量帧，同批 #5 只有 8 个）。接入期带宽短暂升高，
   5 人依次接入时高带宽期约 15 秒。可优化方向：只在"新观众确实缺画面"时才补。

### 14.7 本次新增的验证方法坑（详见 PLAN 约束 37~41）

1. **控制台窗口自摄入**（14.2）—— 差点让整批数据失效，本轮最大的坑。
2. **公平性统计把中途被踢掉的观众算进极差** —— 得到 50% 这种假数字，
   看着像严重不公平，实际在线的那几个极差只有 0.1%。
3. **重试成功的观众用"第一次尝试时刻"当 fps 分母** —— 把 29.6 fps 摊薄成 3.6 fps、
   首帧延迟显示成 14089 ms（实际 21 ms），看着像补位的人被亏待。
4. **`tasklist` / `taskkill` 在 Git Bash 里不加 `MSYS_NO_PATHCONV=1` 会静默失败** ——
   `/FI` 被路径转换吃掉，强杀没执行，一整组实验白跑（e2e30 就这么废掉的）。
5. **压测工具的 `-drop 秒:序号` 是"接通后第 N 秒"**，不是程序启动后第 N 秒。
   给一个还没抢到名额的观众配 `-drop`，它压根没接通，drop 静默不触发，测了个寂寞。

---

## 15. 阶段 5（二）：P0 #1 防自摄入

### 15.1 先证实问题（e2e32）

分享端采**主屏**（`-display 0`），观看端 dump 第 45 帧：

```
goshare.exe -auto share -port 11200 -code SELF1 -preset 最大 -display 0 -exit 30s -log e2e32-host.log
goshare.exe -auto watch -addr 127.0.0.1:11200 -code-watch SELF1 \
            -dump-frame out/selfcapture.png -dump-at 45 -exit 12s
```

`out/selfcapture.png` 里 **GoShare 窗口套了 6~7 层**（LTSC 17763 没有
`WDA_EXCLUDEFROMCAPTURE`，拦不住 DXGI 采集自己的窗口）。

危害不只是观感：
- 每一帧都在变 → **全量帧永远不会停** → 带宽白烧
- 分享端统计：**82 Mbps 持续**，全量帧 9 → 17 稳定增长

### 15.2 实现

`internal/ui/winmin_windows.go`（新）：

1. `EnumWindows` + `GetWindowThreadProcessId == os.Getpid()` + 可见 + 无 owner → 找到主窗
2. `ShowWindow(SW_MINIMIZE)` 最小化
3. 兜底：`SetWindowDisplayAffinity(WDA_MONITOR)` —— 窗口在采集里变黑块
   （17763 有 `WDA_MONITOR`，只是没有 `WDA_EXCLUDEFROMCAPTURE`）
4. `internal/ui/winmin_other.go` 提供非 Windows 空实现，保证跨平台能编译到这个包

调用点在 `startShare` 末尾，独立 goroutine 里**有界重试**（20 × 150ms），
原因见 15.4 的坑 3。

### 15.3 效果（e2e36，同样采主屏）

| | 修复前 e2e32 | 修复后 e2e36 |
|---|---|---|
| 带宽（观众 0 人） | **82 Mbps 持续** | 13 → **0.8 Mbps**（降到静止） |
| 全量帧 | 9 → 17 **持续增长** | **停在 5，之后 460 帧内不再增长** |
| 编码均值 | 16.4 ms | 13.1 ms |

判据用的是"全量帧是否停止增长"和"静止时带宽是否归零" —— **套娃的签名就是"每帧都在变"**，
这两条比肉眼看图更硬（本次验证时也确实没依赖看图）。

窗口状态探针（`spike/winenum`）复核：

```
hwnd=0x1200c08 pid=9868  可见=true  最小化=true  owner=0x0 标题="GoShare · 内网桌面共享"
```

### 15.4 实现过程中踩的三个坑（详见 PLAN 约束 42~44）

1. **`IsWindowVisible` 判断不了最小化**：最小化的窗口仍带 `WS_VISIBLE`，探针显示
   "可见=true 最小化=true"。判据必须是 `IsIconic`；`ShowWindow` 的返回值也只是
   "调用**前**是否可见"，返回 0 不等于失败。
2. **裸 syscall 传 `&pid` 拿不到值**：`proc.Call(hwnd, uintptr(unsafe.Pointer(&pid)))` 里
   pid 恒为 0 → 每个窗口都被判成"别的进程"→ 报"没找到本进程的可见顶层窗口"，
   而窗口明明在屏幕上。改用 `windows.GetWindowThreadProcessId` 封装后立刻正常。
   （定位手段：写了 `spike/winenum` 探针把本进程所有顶层窗口的 pid/可见/owner/标题全打出来，
   一眼看出窗口存在且符合条件，问题只能在"取 pid"这一步。）
3. **`-auto share` 下窗口还不存在**：`startShare` 跑在 `shell.Run(ctx)` 之前，
   所以自动模式必然找不到窗口（日志实测"最小化失败"）。加了有界重试同时兼容两种入口。

### 15.5 残留

- 现在是**任务栏最小化**，不是真托盘：恢复要点任务栏图标。托盘图标（`Shell_NotifyIcon`
  + 自建消息窗口）更利于后台常驻，列入待办。
- 主屏采集的验证永远有自摄入残余风险：**验证过程中自己弹的控制台窗口也会进画面**
  （e2e37 的全量帧就从 3 涨到 42，因为那段时间我在跑探针和读日志）。
  结论：这类验证优先采副屏，或用无窗口工具。

---

## 16. 阶段 5（三）：P0 逐项核对、出网码率、异常场景

### 16.1 P0 十六项逐项核对（结论：15 项落地，1 项缺口）

核对方式是**逐项查代码要证据**，不靠记忆。完整表格见 `PLAN.md` 的"P0 核对"小节。要点：

| 结论 | 项 |
|---|---|
| ✅ 本轮之前就已实现 | #1 防自摄入（上轮）· #2 DPI · #3 光标自绘 · #4 静止心跳 · #5 首帧（MJPEG 天然）· #6 防火墙指引 · #7 多网卡过滤 · #8 授权码防护 · #9 端口顺延 · #11 宽高比 contain · #12 并行转换 · #14 解耦 · #16 分辨率变化 |
| ✅ 框架兜底 | #15 跨 DPI 拖动：Gio 0.10.2 内部处理 `WM_DPICHANGED`（`app/os_windows.go`），不需要自己写 |
| ⚠️ 降级接受 | #10 安全桌面/UAC：有 `Source.Err()` + `BlackScreen()` 检测与提示，**无自动重建**（用户已定锁屏不共享） |
| ❌ **缺口** | **#13 缩放模式可切换**（只有 contain，没有 1:1 原始尺寸 + 平移） |

两个值得记的教训：

1. **`#8 授权码防护` 我一开始判成"没有失败次数限制"** —— 因为 grep 用的是中文关键词
   （"失败次数"/"锁定"/"尝试次数"），而代码里叫 `fails[ip]` / `maxFailPerIP` / `failCooldown`。
   **核对"某功能有没有"时，关键词要覆盖命名风格（英文标识符 + 中文注释），否则会得出"未实现"的假结论。**
2. **`#15` 如果是"我们自己不知道要不要做"，先去框架源码里查一眼**。
   `WM_DPICHANGED` 在项目里 grep 是 0 命中，看起来像"完全没做"，
   实际是 Gio 内部处理了 —— 差点白写一遍 Win32 消息。

### 16.2 HUD 的 Mbps 改成"出网 / 单路"双口径

**问题**：原来 HUD 显示的是 `Stats.Bytes` 差分，那是**单份编码**的字节。
N 个观众要各发一份，所以显示 3.37 Mbps 时实际出网是 16.87 Mbps —— **低估 5 倍**。

**改法**：`Stats` 新增 `SentBytes` / `SentFrames`，在 `emit` 的发送循环里按
"`SendFrame` 返回 nil"累加（真正发出去的才算，背压丢掉的**不该算**）。

**实测**（e2e40，5 人，副屏）：

| 口径 | 数值 |
|---|---|
| 分享端出网 | **16.87 Mbps** |
| 分享端单路编码 | 3.37 Mbps |
| 观众侧实测每人 | **3.14 ~ 3.40 Mbps** |

`16.87 / 5 = 3.37`，与观众侧实测逐人对上 —— **跨侧自洽**，口径正确。

日志格式同步改为：`分享统计：30.0 fps · 出网 16.87 Mbps（单路 3.37 × 5 人）· …`

### 16.3 分享端停止后观众侧的提示（本轮新增功能）

**改前**：断开后画面**冻结在最后一帧**（`lastFrame` 不清空），界面只有 fps 归零。
用户看到一张静止的图，**分不清"对方停止分享了"、"自己网络断了"还是"画面本来就不动"**。

**改后**：观看端监听 ICE 状态（`rtc.Config.OnState`，只做记录与打日志 —— 约束 8 要求回调里
绝不能做会等待 ICE 的动作），按情况给两种话术，并叠一条提示带在画面底部：

| 情况 | 提示 |
|---|---|
| ICE `failed`/`closed`/`disconnected` 连续 ≥1.5s | `连接已断开（<状态>）—— 对方可能已停止分享，或网络中断`（警告色） |
| ICE 正常但 5s 无帧 | `画面已超过 5 秒没有更新 —— 对方可能已暂停分享` |

5s 这个阈值有依据：**静止桌面本来就有 2s 心跳帧保活**，所以"无帧 5s"本身就异常。

三种断开方式实测：

| 场景 | 实测（e2e） | 结果 |
|---|---|---|
| 分享端正常停止（`-exit`，发 close） | e2e45 | 16:03:13 `connected → closed` → 16:03:14 提示上屏 ✅ |
| 分享端**进程被强杀**（不发 Bye） | e2e46 / e2e47 | `connected → disconnected`（1.5s 内提示）→ 24s 后 `→ failed` 并更新话术 ✅ |

### 16.4 分享中热切换采集区域（e2e41，2 人）

后端本来就有 `Sharer.SetRegion`（内部 `src.SetRegion` + `ForceKeyFrame`），缺运行时入口。
先补自动化开关（`-switch-at 12s -switch-region 800x600+100+100`），走的正是界面该走的那条路。

| | 切换前 1920×1080 | 切换后 800×600 |
|---|---|---|
| 观众侧那一帧 | 全量 17/17 非黑 98.0% | **全量 19/19 800x600 非黑 99.7% 黑带 0** |
| fps | 30.0 | **30.0**（不断流） |
| 编码均值 | 6.6 ms | 3.4 → **3.1 ms** |
| 单路码率 | 9.05 Mbps | **0.78 Mbps** |
| 全量帧计数 | 11 → 14（切换触发） | 之后 **14 恒定不再增长** |

切换后**没有黑带、没有残影、没有丢帧**。关键在 `ForceKeyFrame`：
区域变了观众侧画布尺寸也变了，增量帧只覆盖"变化过的条带"，
尺寸变化后旧画布上没被覆盖的区域会残留上一块区域的画面（与 R32 同一类问题）。

### 16.5 ⚠️ 本轮最大的坑：`ExitAfter` 被"窗口最小化"饿死（约束 45）

做 16.3 的验证时，连续两轮都看不到"分享端停止 → 观看端感知"，
`connected → closed` 两次都**恰好等于观看端自己的退出时刻**（+42s / +65s）。
差点得出"ICE 断开检测不灵敏"的结论。

真相是**分享端压根没退出**：`-exit 22s` 实际跑了 **83 秒**，日志写着"共渲染 **6 帧**"。

根因：`ExitAfter` 的检查写在 `app.FrameEvent` 里，而**防自摄入会把主窗最小化** ——
最小化后 Gio 不再产生帧事件，检查被彻底饿死。

危害远超"验证不准"：

| 后果 | 说明 |
|---|---|
| 端口被占 | 残留的分享端还监听着信令端口，新实验的端口顺延 + 连到旧进程，**"握手成功"掩盖一切** |
| 白烧资源 | 持续采集 + 编码 30fps，1.1 GB 内存，而它看起来"什么都没做" |
| 实验污染 | 我把两轮的"断开没被感知"误判成 ICE 检测有问题，而去查一个**本来就正确的模块** |

修法：独立 `time.AfterFunc` 驱动；关窗动作抽成唯一的 `closeNow`
（`atomic.Bool.CompareAndSwap` 保证只关一次）；`frames` 改 `atomic.Int64`。
**实测修复后 `-exit 25s` → 26 秒退出**（渲染仍只有 5 帧，证明定时器确实独立于渲染）。

### 16.6 顺带修掉的一处"日志谎报"（约束 46）

e2e46 的日志里出现：

```
16:04:13 观看提示上屏：画面已超过 5 秒没有更新 —— 对方可能已暂停分享
16:04:15 观看连接状态：connected → disconnected
16:04:15 观看提示已清除：画面恢复正常          ← 谎报！画面明明是死的
16:04:16 观看提示上屏：连接已断开（disconnected）
```

原因：ICE 刚转 `disconnected` 时防抖计数还没到 3，`Notice` 被清空，
而"提示翻转"的日志把"清空"直接翻译成了"画面恢复正常"。

修法两条：① 防抖期内**沿用**上一条提示，不要凭空清空；
② "恢复"的措辞绑定事实 —— 只有最近确实收到帧才说"恢复更新"，
否则输出"提示已清除，但最近一帧已是 N 秒前 —— 画面并未恢复，只是判据切换"。

（修复后 e2e47 复测通过；但那一轮强杀后 4 秒就 `disconnected` 了，
没经过"停更 → 断开"的中间态，所以**"沿用"分支未再单独复现到**，不宣称已单独验证。）

### 16.7 本轮新增的验证方法坑（约束 45~47）

1. **渲染帧驱动的定时逻辑会被"窗口不可见"饿死**（16.5）—— 本轮最大的坑。
2. **"提示消失"不等于"画面恢复"**（16.6）—— 日志措辞要绑定事实，不要绑定变量状态。
3. **同一文件的多个编辑不能并行下发**：本轮踩了 4 次。最隐蔽的一次是
   `Stats` 新增字段 / `emit` 累加 / HUD 显示三处改动里**两处被静默丢弃** ——
   `go build` 通过、接口对得上，只是**出网码率恒为 0.00**。
   教训：**改完必须 grep 回来看一眼是否真的落地，不能只看编辑工具返回"成功"**。
